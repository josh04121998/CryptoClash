# CRYPTO CLASH — Status

### Last updated: 2026-09-10 (session 19)

---

This is a living document — read it for "where are we right now," and read `spec.md` / `batlleSpec.md` / `architecture.md` for "what are we building toward" and `branding.md` for "what does it look like." Update this file, don't accumulate a second one, whenever a milestone lands or the plan changes.

# 0. Project Direction (read this first)

The end goal, per the user (2026-09-04): **bring the Pokémon-collectibles experience on-chain** — collecting/chasing rarity is the *main* draw, not a side layer bolted onto a card game. Battle gameplay (engine, factions, matches) is the vehicle; the thing players are meant to get hooked on is owning, showing off, and trading cards.

Go-to-market is crypto-native: launch via a token, acquire early users from the crypto/Web3 community rather than a broad mainstream audience first. Consequence: **wallet-connect is the primary identity system from day one** (not email/social-first-then-wallet-later, which is what `architecture.md` Section 9 originally recommended — see Section 5 below for that reconciliation). Guiding principle from the user: *least possible friction to just play* — no login wall in front of Play vs AI or Play Online; a wallet is only asked for the moment something needs to persist (a saved deck, eventually a collection/NFT).

Explicit non-goal: literal Hearthstone production values (painted art, voice, orchestral audio, live-ops team) — not achievable solo/small-team and not the point. The bar is "complete, fun, well-balanced, genuinely collectible," not "AAA."

**Open, unresolved as of session 12 (2026-09-08): the user is reconsidering the game's actual name**, "Crypto Clash" — raised in passing during a branding/asset session, not yet discussed in depth. Until this is settled: don't bake the current name into anything that would be expensive to redo (character art, a finalized logo lockup) — see `branding.md` Section 1.

**Token/economy direction, settled 2026-09-07 (not yet built — design target for spec.md/architecture.md, see those files for the full reasoning):** Coins stay exactly as designed (free, earn-only, gates the core loop) — real-money/stablecoin pack purchases become a *separate* cash-shop path alongside them (spec.md Section 21), the industry-normal "whales buy more shots at rares" lever, deliberately never routed through the token so pack pricing isn't exposed to its volatility. The external token is a marketing/community/staking layer riding this cycle's memefi wave, thematically anchored to the Doggos faction (dogs being the proven meme-coin lineage) — any staking rewards or buybacks must be funded from real revenue (a cut of cash-shop pack sales + marketplace fees, spec.md Section 27's new Funding Rule), never from token emissions, specifically to avoid the Axie Infinity SLP collapse pattern (emission-funded rewards only hold up while new-buyer inflow outpaces emission — structurally unsustainable once growth slows). Leaning toward self-listing the token (own contract, own seeded liquidity pool, e.g. Uniswap/Aerodrome on Base) over a pump.fun-style launchpad, conditional on pairing it with real distribution (the game + Doggos meme identity + an airdrop to early players, not just hoping a DEX listing draws attention on its own) and visibly locking liquidity to preempt rug suspicion — see architecture.md Section 8's new subsection. Exact tokenomics (supply, allocation, launch timing) still open.

**Chain choice, discussed 2026-09-08 (session 9) — leaning Robinhood Chain, not locked in:** the user asked for a pros/cons comparison against Solana. Robinhood Chain is an Ethereum-compatible Arbitrum L2 (mainnet launched 2026-07-01, settles to Ethereum, OpenSea/Uniswap/Chainlink integrated day one) — it inherits the exact "EVM L2, mature tooling" reasoning `architecture.md` Section 12 already used to favor Base/Polygon over alternatives, at essentially zero extra engineering cost, since the whole wallet-connect stack (`ethers`/`siwe`/EIP-1193/`personal_sign`) already speaks EVM. Solana would mean rebuilding that already-shipped, already-tested auth layer from scratch (different wallet model, ed25519 signing, no SIWE equivalent) — a real cost, not a preference, that tipped the recommendation. Real risk acknowledged, not dismissed: Robinhood Chain had only ~2 months of mainnet history at the time of this decision, so its early NFT-volume numbers (~$1.05M/day) may be launch-week energy rather than durable traction; Solana's NFT ecosystem, despite the user's (well-founded, current) scam-reputation concern, is far more battle-tested. **Still not built** — `architecture.md` Section 12's own guidance (don't build the on-chain layer before the game/economy is proven) still applies; this is a direction, not a commitment, and nothing stops building the Web3 service chain-agnostically per the architecture doc's existing recommendation and deciding for real at deploy time.

---

# 1. What Exists Right Now

A monorepo (npm workspaces) with four packages, all live and deployed:

| Package | Role | Status |
|---|---|---|
| `engine/` | Headless, deterministic battle engine | Core systems complete |
| `shared/` (`@cryptoclash/protocol`) | Client/server wire format + (de)serialization | Complete for current scope |
| `server/` | WebSocket match server + accounts/decks REST API | Working, deployed (API needs prod DB env vars — see Section 4) |
| `client/` | React web app (mobile-responsive) | Two play modes + wallet-connect + deck builder live |

**Live deployments:**
- Client: `crypto-clash-client-six.vercel.app` (Vercel, auto-deploys on push to `main`, Root Directory `client`)
- Match server: `vivacious-passion-production-1a17.up.railway.app` (Railway, auto-deploys on push to `main`, Root Directory repo root)
- Repo: `github.com/josh04121998/CryptoClash`
- **Now live in production** (2026-09-05): Railway Postgres provisioned, migration applied, `DATABASE_URL`/`JWT_SECRET`/`SIWE_DOMAIN`/`CLIENT_ORIGIN` set on the `vivacious-passion` Railway service. Verified end-to-end against the deployed site with a scripted wallet (Playwright + a mock EIP-1193 provider signing with `ethers`, since there's no real MetaMask in a headless run): connect wallet → SIWE sign/verify → build a legal 30-card deck → save → persists. Found and fixed one real bug in the process: `client/src/api.ts`'s `apiUrl()` didn't strip a trailing slash from `VITE_SERVER_URL`, so `${apiUrl()}${path}` produced a double slash that 404'd before the server's CORS headers could attach — every `/api/*` call from the browser was silently failing with a CORS error, not the `503` the missing-env-var path would give. Fixed by stripping trailing slashes in `apiUrl()` regardless of how the env var is set.

**Test coverage:** 56 engine tests, 122 server tests (across `auth.test.ts`, `db.test.ts`, `api.test.ts`, `server.test.ts` — the latter three are real-Postgres integration tests that skip gracefully without `DATABASE_URL`, verified green against a live local Postgres session 17 and again session 19), **37 client tests (new this session, session 19 — see Section 8; the client had zero committed automated tests before this)**, all passing. Client type-checks clean and builds clean. No test suite for `shared` (it's pure data transforms, covered indirectly by the server integration tests).

---

# 2. Battle Engine (`engine/`)

Implements batlleSpec.md's full "First Prototype" checklist (Section 32), **Items included**:

- Turn loop: Energy (1→10, refills each turn), Draw, Play, Attack, End
- 5-slot board with adjacency (Section 5/8) — auras key off it (Moon Dog's "+1 Attack while next to another Doggo")
- Creatures, Spells, **and Items** (equip-style buffs/keyword grants — see below)
- All five launch keywords (Section 11): **Rush**, **Guard**, **Stealth**, **Burn**, **HODL**
- **Volatility & all five Market Events** (Sections 16-18): MARKET_CRASH, PUMP, LIQUIDATION, FOMO, BLACK_SWAN
- Fatigue (escalating damage on empty-deck draw), hand size cap
- Server-authoritative by construction: the engine is pure/deterministic (`seed` + ordered `Intent`s → identical result every time), so replay/anti-cheat (Section 31) falls out for free — `state.log` already is that replay

**Card pool:** 73 templates. All six spec.md Section 6 factions now have real depth and their own standalone 30-card deck (`cards.ts`, indexed by `DECKS`/`getDeck()`):

| Faction | Deck export | Signature mechanic |
|---|---|---|
| Doggos | `SAMPLE_DECK` | Swarm/adjacency (existing) |
| Frogs | `FROG_SAMPLE_DECK` | `copyRandomFriendly` — copy a random friendly creature |
| Builders | `BUILDER_SAMPLE_DECK` | `draw` — card advantage/combo |
| Degens | `DEGEN_SAMPLE_DECK` | `damage` targeted at `selfPlayer` — pay your own HP for power |
| Crypto Bros | `CRYPTOBRO_SAMPLE_DECK` | `gainEnergy` (this-turn burst) / `gainMaxEnergy` (permanent ramp) |
| Normies | `NORMIE_SAMPLE_DECK` | `heal` — steady, defensive toolkit |

Plus **5 Neutral Items** (`buffTarget` for permanent +stat upgrades, `grantKeywordTarget` for temporary Rush/Guard grants — Guard/Rush are the only keywords `combat.ts` checks generically via `keywords ∪ tempKeywords`, so those are the only two safe to grant this way).

**Session 4:** every non-token template carries a `rarity` (spec.md Section 13's tiers — Common through Genesis), assigned by a simple cost-based heuristic (cost 0-1→Common, 2→Uncommon, 3→Rare, 4→Epic, 5→Legendary; no current template uses Mythic/Genesis, reserved for scarcer future content). It's gameplay-adjacent metadata only — never read by combat/effect resolution, purely there for pack odds and collection-screen grouping. The heuristic is a placeholder, not tuned game/economy design.

**New this session (session 5) — a real bot opponent (`bot.ts`):** `takeBotTurn` was a pure greedy heuristic (play whatever's affordable into the first empty slot, always attack face unless a Guard forces otherwise) — now it actually reasons about combat and positioning, while staying the same fast/synchronous/zero-extra-randomness function the engine's determinism guarantee depends on. It computes real lethal (sum of `getEffectiveAttack` across everything still eligible to attack ≥ enemy HP) and only goes face-first when that's true; otherwise it looks for a "clean kill" — an enemy creature it can kill without dying itself, preferring the highest-effective-attack option when more than one qualifies — and only falls back to face damage when no good trade exists. Creature placement now prefers an empty slot adjacent to an existing friendly creature when that would actually trigger that neighbor's `adjacentSameFaction` aura (e.g. landing next to Moon Dog), instead of always taking the first empty slot. Hand-play order changed too: cards are now tried biggest-cost-first each pass rather than hand-index order, so a big card doesn't get stranded behind a small one that happened to be drawn first. 3 new tests in `engine/test/engine.test.ts` (clean-kill-over-face, still-face-when-lethal, aura-aware placement) plus the existing 42 — 45/45 passing. Sanity-simulated 200 bot-vs-bot games across all 6 faction decks and mixed seeds with no exceptions.

**New this session (session 16) — game balance/mechanics pass, driven by real playtesting data:** the user reported the game "seems mindless... you can play any card and not really have to think," asked for actual playtesting rather than guesswork, flagged Degens as feeling overpowered, and asked directly whether Hearthstone/Yu-Gi-Oh-style mechanics (abilities, trap cards) could help. Investigated with headless bot-vs-bot simulation instead of speculating:
- **Root cause #1 — energy/cost-curve mismatch:** max Energy ramps to 10, but the highest card cost in the whole pool was 5 — a 180-game simulation showed 40-83% of turns from turn 11 onward where a player could afford their *entire hand*, meaning "what to play" stopped being a real decision well before most games ended.
- **Root cause #2 — a self-caught bad first metric:** an initial "how many cards are vanilla (no effect)" check only looked at `effects.length > 0`, misclassifying keyword-only cards (a bare Rush/Guard grant) as non-vanilla and producing a misleading "40% vanilla, Doggos 78%" headline. Corrected (also excluding `keywords`/`aura`) to the real number: 11% true vanilla — the roster already leaned on combinations, not repetition, contrary to the first pass.
- **The "Degens feel overpowered" complaint, reconciled against contradictory sim data:** a 600-game win-rate sweep across all faction matchups actually showed Degens as the *weakest* faction in bot-vs-bot play (36.5%) while Normies (71%) and Builders (63%) were overtuned. Both things are true at once — the bot AI has no self-damage-risk model, so it systematically misplays a high-skill-ceiling "pay your own HP" archetype a human plays much better; this is a real, separately-flagged bot-AI gap, not a reason to dismiss either data point.
- **Hearthstone/Yu-Gi-Oh research → 4 approved changes:** researched from established game-design knowledge (no external source needed), scoped by architectural fit rather than copying wholesale. Full Yu-Gi-Oh Trap Cards (opponent-chosen activation with a priority/response window) were ruled out as too large a turn-structure change for now; Hearthstone-style **Secrets** (automatic-trigger hidden cards, no player choice at reveal) were the tractable middle ground. User approved all four:
  1. **Deathrattle** (`onDeath` trigger) — a new pending-queue pattern (`MatchState.pendingDeathrattles`, drained in `effects.ts`'s `processPendingDeathrattles`) avoids a circular import between `matchOps.ts` (low-level, no upward deps) and `effects.ts`; drains in a loop so a Deathrattle that kills a second Deathrattle creature chains correctly within one `applyIntent`.
  2. **Secrets** (`CardType: "Secret"`) — armed face-down via `playCard`, fired automatically by `checkAndFireSecret` at the two moments that matter (`combat.ts`'s `resolveAttack`, before damage; `engine.ts`'s `playCard`, after an enemy creature lands), one reveal per event, consumed on fire. **Documented, not fixed, gap:** the wire protocol (`shared/src/index.ts`) already sends full `MatchState` — including the opponent's whole hand and deck order — to both players in a Play Online match, so `PlayerState.secrets` is exposed the same way; real per-viewer redaction is a separate, bigger feature.
  3. **Top-end finishers** — one 6-8 cost Legendary per faction (Exit Liquidity, Unicorn Exit, Unicorn Startup, Alpha Dog, Primordial Croak), raising the pool's max cost from 5 to 8 — closer to the 10-Energy ceiling, restoring late-game resource tension.
  4. **A balance pass**: Normies' heal (Rainy Day Fund 5→4) and stats (Steadfast Normie 7→6 HP) trimmed, plus a Guard-density cut (Safe Harbor 3→2 copies); Builders' draw density cut (Blueprint, Efficient Engineer 3→2 copies each); Degens' Margin Call (1→2 cost) and Moonshot (7→6 Attack) trimmed, plus 2 new cards (Exit Liquidity, Short Position — the first Secret) added to compensate; Crypto Bros got their first real ramp payoff (Unicorn Exit); a new **Silence** `EffectAction`/mechanic (Audit Trail) added as answer tech, stripping keywords/aura contribution going forward without retroactively undoing stat buffs already granted.
  - All six `*_SAMPLE_DECK` arrays updated to stay exactly 30 cards while fitting every new card and trim. Re-ran both simulations after the changes: the win-rate sweep moved meaningfully toward parity (Normies 71%→62%, Builders 63%→57.8%, CryptoBros 38%→45.8%); Degens' bot win rate barely moved (36.8%) despite real card-power gains, consistent with the AI-misplay explanation above rather than a card-balance failure — worth revisiting once/if the bot AI learns to manage self-damage risk. The afford-whole-hand curve softened materially in the turn-4–7 range (was the "mindless" window) now that a real 8-cost ceiling exists.
  - 12 new engine tests (Deathrattle including a same-trade double-Deathrattle chain and a silenced-creature suppression case; Silence including the "doesn't undo an existing buff" case; Secrets for both trigger types including the "attack cancelled if the attacker dies to the secret" case) — 53/53 engine tests green, `tsc --noEmit` clean. `card-schema.md` gained a new Section 8 (Secrets) and updated Sections 2-4/9 for the new trigger/type/selector/action/gap.

**New this session (session 17) — bot self-damage-risk model (`bot.ts`):** follow-up to session 16's finding that Degens under-reports its real strength in bot-vs-bot play because the bot has no concept that a self-damage card is risky. Added `isSelfDamageTooRisky`: the bot now skips an `onPlay` self-damage card when it would leave its own HP at/below a ~25%-of-max floor or at/below the enemy board's current full-swing attack total — unless the card closes out a lethal swing outright, in which case it plays it anyway. Stays pure/synchronous (no new randomness). Validated with the same 600-game bot-vs-bot sweep session 16 used: Degens' win rate moved 37.5%→40.0%, a real but modest gain — a follow-up diagnostic found the *dominant* Degens bot-loss pattern isn't self-inflicted death at all, it's low-HP/high-attack creatures dying in trades and leaving the board empty against sustain factions (Normies' healing especially) — a broader trade-evaluation/board-recovery gap, out of scope for this fix, worth a future session. 3 new tests (declines a lethal-risk self-damage play with nothing to gain, still plays it when it's lethal anyway, declines a play that would step into the enemy's current lethal-swing range) — 56/56 engine tests green.

See `card-schema.md` for the full effect DSL (now 14 `EffectAction` kinds), and `engine/README.md` for how to run/extend it.

---

# 3. Client (`client/`)

Two play modes, both live, both fronted by a **deck picker** (`DeckPicker.tsx`) — pick a starter deck, or (if wallet-connected) one of your own saved decks, before a match starts:

- **Play vs AI** — runs the engine directly in the browser against a greedy-heuristic bot (`takeBotTurn`), which now also picks a random deck each match for variety. No backend, no wallet required. This is the permanent practice/tutorial mode, not a placeholder.
- **Play Online** — real matchmaking through the Railway match server; the resolved 30-card list (starter or custom) rides along on the `findMatch` message — the server re-validates it (`validateDeck`) rather than trusting the client, falling back to the default deck if it's missing or illegal.

Board/hand/interaction UI (`MatchView`) is shared between both modes, parameterized by which player is "me," so every fix or feature (the Volatility meter, keyword badges, etc.) applies to both at once. Targeting distinguishes friendly-target cards (Items — click your own board) from enemy-target cards (damage/burn spells — click the opponent's board/portrait), via the shared `targetsFriendlyCreature()` helper (also used by the bot, so it doesn't waste Items targeting the wrong side).

**New this session (session 8) — a real visual identity + a landing page:** raised directly by the user — the UI "looks very basic and very AI," and the game had no marketing/hype page at all. Design direction settled with the user first (STATUS.md flagged this exact decision as open back in session 5's roadmap item 9 — "art direction... a real decision to raise with the user, not decided unilaterally"): **"the trading floor is the battlefield"** — a dark Wall Street/bullpen aesthetic (terminal green, ticker gold, electric cyan readouts, a lit NYC skyline) doubling as the game's combat identity, chosen because the whole cast (Degens, Crypto Bros, Builders) is already trading-culture themed, not generic fantasy-war visuals.
- **Design tokens** (`styles.css` `:root`): new palette (`--bg`/`--panel`/`--border` darker and green-tinted, `--accent` ticker gold, `--accent-2` terminal green, `--energy` electric cyan kept distinct from Coins gold — fixed a pre-existing mismatch where Coins reused the Energy color var), plus `--font-display` (Chakra Petch, a squared technical face — Google Fonts, loaded in `index.html`) for headlines/branding and `--font-mono` (IBM Plex Mono) for anything that reads like a terminal readout (HP, Energy, Coins, leaderboard numbers). Because ~118 of the stylesheet's rules already referenced these CSS custom properties rather than hardcoded hex, changing the `:root` values alone re-skinned every existing screen (menu, match board, leaderboard, quests, packs, crafting) consistently for free — only the app-bar and main menu needed hands-on touch-ups (glow borders, the new fonts, a hover-lift treatment on menu cards).
- **`LandingPage.tsx`** (new, and the app's new default screen — `App.tsx`'s `mode` now starts at `"landing"` instead of `"menu"`): a hero ("The floor is the battlefield.") over a lit inline-SVG NYC skyline silhouette (pure CSS/SVG, no image asset) and a scanline/glow backdrop, a scrolling `TickerTape.tsx` strip of in-theme flavor lines ("BLACK SWAN EVENT DETECTED", "GENESIS SUPPLY: PERMANENTLY CAPPED"), a 3-pillar Collect/Battle/Trade section, and a community section. Community links (Discord/X/Telegram) are placeholders per the user's explicit choice this session — rendered as clearly-labeled "Soon" badges, not real hrefs, since no real community channels exist yet; swap in real URLs there once they do. The `CRYPTO CLASH` logo in the main menu's app-bar is now a button back to the landing page.
- Verified visually with real Chromium screenshots (Playwright, desktop 1400px + mobile 420px viewports) across the landing page, main menu, leaderboard, and a live Play vs AI match board — confirmed the skyline/ticker render correctly (an initial pass had the skyline silhouette blending invisibly into the background; fixed with a brighter fill, a stroke outline, and lit-window highlights), the palette cascades correctly into in-match UI (cards/board/turn-indicator all inherited the new gold/green identity with zero per-component edits), and mobile layout holds up. **Not yet extended:** the battle-board's card art/frame layout itself is unchanged (only re-colored) — a fuller card-frame redesign, sound, and any illustrated/generated art assets are still open (see roadmap).

**New this session (session 9) — Invite Friends (`ReferralScreen.tsx`, `referral.ts`):** a viral-growth loop the user asked for directly — see Section 4 for the full server-side writeup (both sides get a free pack; the referrer's is gated on the referred friend completing a real match). Client-side: a "?ref=CODE" URL param is captured into `localStorage` at app boot and sent along with the next sign-in (one-shot, cleared regardless of outcome); "Invite Friends" in the main menu shows the shareable link, a Copy button, and live Invited/Free-Packs-Earned/Awaiting-First-Match counts. Verified with a 3-wallet scripted-browser run — see Section 4 for the details.

**Wallet-connect + deck builder** (`useWallet.ts`, `DeckBuilder.tsx`, `MyDecksScreen.tsx`; session 3):
- "Connect Wallet" in the top-right of the main menu — [Sign-In with Ethereum](https://eips.ethereum.org/EIPS/eip-4361) against any injected browser wallet (MetaMask etc.), verified end-to-end with a real cryptographic signature flow. No login wall anywhere else — every other screen works with no wallet connected.
- Once connected: "My Decks" screen (list/create/edit/delete) and a full deck builder — browse the pool, add/remove with a live `validateDeck()` legality check (exactly 30 cards, max 3 copies, no tokens), save. Saved decks are real Postgres rows, scoped per-wallet, and appear back in the match deck-picker.
- Bundle-size cost: `siwe`/`ethers` (needed for SIWE message construction/signing) added ~670KB gzipped-~95KB to the client bundle, and required a Node `Buffer` polyfill (`vite-plugin-node-polyfills`, scoped to just `buffer`+`process` — deliberately *not* `crypto`, which would've pulled in a vulnerable `elliptic` transitive dependency for a polyfill nothing here needs). Worth revisiting in a later polish pass if load time becomes a concern.
- **Real bug, found via live-site testing after this session's deploy (2026-09-07):** connecting with a real wallet threw `invalid EIP-55 address` and never got past "Connect Wallet." `useWallet.ts` was passing `eth_requestAccounts`'s address straight into `SiweMessage` — `siwe`'s own validation requires an EIP-55-checksummed address and throws otherwise, but not every wallet returns one already checksummed (this one apparently returned all-lowercase). Fixed by normalizing with `ethers`'s `getAddress()` right after reading the account. `ethers` was already an implicit, hoisted dependency (a `siwe` peer dependency, already counted in the bundle-size note above) — made explicit in `client/package.json` rather than relying on hoisting from a sibling workspace's devDependency. Side effect: the client bundle actually *shrank* (894KB→776KB minified) once Vite could resolve/tree-shake `ethers` from one clean explicit dependency instead of whatever implicit path it was finding before. Verified with two scripted-browser checks: the exact reported address no longer throws (fails later, as expected, only because the mock signature isn't real), and a full real-wallet sign-in with a deliberately lowercased address succeeds end-to-end.

**Session 4 — ownership-gated deck building:** `DeckBuilder.tsx` fetches `/api/collection` on mount and caps every card's addable copies at what the account actually owns (not just the flat 3-copy rule) — a pool card with 0 owned copies renders dimmed and its Add button disables; the sidebar's legality check folds in an ownership check client-side too (mirroring the server's `validateOwnership`), so Save disables proactively instead of round-tripping to a 422. This was invisible in practice at the time (`grantStartingCollection` gave every account the whole pool) — session 5's narrower starting grant is what finally makes it visible.

**Session 5 — Coins + Packs (`PacksScreen.tsx`, `rarityColor.ts`):** a wallet-connected player sees a live 🪙 Coins balance in the app bar and a "Packs" screen — browse pack definitions (`GET /api/packs`), open one (`POST /api/packs/open`), and watch a staggered reveal (one card flips in every 350ms, not a blank grid dumped instantly — spec.md Section 17 calls this "one of the game's signature moments") with each card's rarity shown as a colored corner dot (`CardFace.tsx`, `template.rarity` badge). Opening updates the app-bar balance immediately and the newly-owned cards show up un-dimmed back in `DeckBuilder` on the very next visit, no extra fetch/refresh logic needed since it re-reads `/api/collection` on mount. Verified end-to-end in a real Chromium browser (Playwright + a mock EIP-1193 provider signing with `ethers`, same technique as session 4's live-site check — the Claude-in-Chrome extension wasn't connected this session): connected a scripted wallet, saw the 3,000-Coin welcome bonus, opened a Standard Pack, watched all 5 cards reveal and the balance drop to 2,000, then confirmed the pulled cards appeared un-dimmed in the deck builder afterward.

**New this session (session 5) — Collection screen (`CollectionScreen.tsx`):** the "digital binder" from spec.md Section 19 — a wallet-connected player sees every non-token template (grouped/filterable by faction and rarity, plus an "Owned only" toggle), each rendered via the same `CardFace` used everywhere else, dimmed if unowned with an "N owned"/"Not owned" caption underneath. A summary bar up top shows total owned/total templates plus a per-rarity breakdown ("Common 18/18 · Uncommon 3/21 · …", each label tinted by `rarityColor`). Deliberately doesn't expose First Edition/Foil/Serial filters yet (spec.md Section 19 lists them) — nothing populates `card_instances` with anything but the `standard` edition today, so those columns exist in the schema but have no real data behind them; the screen only surfaces what's actually true. Verified end-to-end the same way as Packs: scripted wallet, opened 2 packs for some Uncommon+ pulls on top of the Common starter set, confirmed the summary math (24/63 owned, per-rarity counts) and every filter combination (faction, rarity, owned-only, and stacked) against the real card grid.

**New this session (session 6) — real pack/rarity economy design + foils (`PacksScreen.tsx`, `CollectionScreen.tsx`, `CardFace.tsx`, `styles.css`):** replaced the "everything's basically Common" placeholder odds with a real weighted table, and shipped a second, independent collectibility axis — cosmetic foils — landing the first of Section 19's dormant filters. See Section 4 for the server-side design (odds, the foil roll, the Mythic/Genesis exclusion) and spec.md Section 17 for the full rationale. Client-side: `CardFace` gained a `foil` prop rendering an animated rainbow-gradient border (pure CSS — `border-box`/`padding-box` dual-background trick, no art asset); `PacksScreen`'s reveal shows a "✨ Foil" tag under any foil pull; `CollectionScreen` now fetches and shows a foil count in the summary bar, a per-card "✨N foil" badge, and a "Foils only" filter alongside the existing "Owned only" one. First Edition/Serial filters still aren't shown — nothing grants those yet (see Section 4). Verified end-to-end with a real Chromium browser (Playwright + a mock EIP-1193 provider signing with `ethers`, same technique as prior sessions — Claude-in-Chrome wasn't connected this session either): scripted wallet, topped up Coins directly in Postgres to afford enough packs to reliably hit the 8% foil roll, opened packs until a foil landed (took 5 packs this run), confirmed the reveal screen's foil shimmer + "✨ Foil" tag, then confirmed the Collection screen's "✨ 1 foils" summary count and that "Foils only" narrowed the grid to exactly that one card.

**New this session (session 6) — Crafting screen (`CraftingScreen.tsx`):** a wallet-connected player can reach "Crafting" from the main menu — every Uncommon+ template (Commons and Mythic/Genesis excluded, see Section 4) shown via the usual `CardFace`, with an owned count, a qty-selectable "Disenchant → N 💠" action, and a "Craft — N 💠" action gated on the account's Dust balance. Dust values come from `GET /api/craft/rates`, not hardcoded client-side (same reasoning `PacksScreen` fetches `PACK_DEFINITIONS` rather than duplicating them). A 💠 Dust balance shows in this screen's own app bar, mirroring how `PacksScreen` shows Coins — deliberately not added to every screen's global app bar, since Dust (unlike Coins) is only relevant here. Verified end-to-end with a scripted-browser Playwright run: granted 5 duplicate Rares directly via Postgres, disenchanted all 5 for 100 Dust, crafted a different Rare for exactly that 100, and confirmed the Collection screen reflected the new card and the lost duplicates.

**New this session (session 7) — Quests + Daily Login (`QuestsScreen.tsx`):** the Coins-earn loop's second half (spec.md Section 21's "Quests"/"Daily rewards" — session 5 already covers "Playing"/"Winning" via match results). A wallet-connected player reaches "Quests" from the main menu: a Daily Login card (current streak, that streak-day's Coins reward, a Claim button that disables once claimed for the UTC day) above a list of today's 3 quests (Play 1/Play 3/Win 1 match, each with a progress bar and its own Claim button that only enables once its goal is met). Both fetch their definitions/rates from the server (`GET /api/daily`, `GET /api/quests`) rather than hardcoding numbers client-side, same reasoning `PacksScreen`/`CraftingScreen` do. Verified end-to-end with a scripted-wallet Playwright run: connected, opened Quests, confirmed all 3 quests render at 0/goal with disabled Claim buttons, claimed the Daily Login reward (Coins balance 3000→3050, matching day-1's reward), reloaded the page and confirmed "Claimed" persisted (server-side state, not client-only), and confirmed re-authorizing via the new "Switch Wallet" button and Disconnect both work cleanly.

**New this session (session 7) — wallet-switching (`useWallet.ts`):** raised by the user mid-session as a real gap — `connect()`'s `eth_requestAccounts` won't show MetaMask's account picker once a site is already authorized, and there was no listener for the active account changing outside the app. Fixed two ways: (1) a `switchWallet()` action, wired to a renamed "Switch Wallet" button (previously the connected-address button doubled as Disconnect — now split into two: clicking the address forces `wallet_requestPermissions` to reopen the picker, then re-runs the normal SIWE connect flow for whichever account is chosen; a separate "Disconnect" button does what the old combined button did); (2) an `accountsChanged` event listener — since the session token is SIWE-signed for one specific address, a mid-session account switch inside the wallet extension now clears the stale session and prompts "click Connect Wallet to sign in with it" rather than silently continuing to act as the old account. Deliberately doesn't auto-re-sign on `accountsChanged` — popping an unsolicited signature prompt the instant someone switches accounts for an unrelated reason would be surprising.

**New this session (session 10) — multi-wallet picker (`useWallet.ts`, `WalletPicker.tsx`):** session 7's wallet-switching fix turned out to be half the story — it could re-prompt for a different *account* within whichever extension `window.ethereum` happened to point at, but with more than one wallet extension installed (MetaMask + Coinbase Wallet + Rabby, etc.) there was never a way to pick *which extension* at all, since `window.ethereum` is just whichever one claimed that global first. Fixed with [EIP-6963](https://eips.ethereum.org/EIPS/eip-6963) "Multi Injected Provider Discovery": the app now listens for `eip6963:announceProvider` events (and fires `eip6963:requestProvider` on mount to catch wallets that already announced before the listener attached) to enumerate every installed wallet, tracked in `useWallet.ts`'s new `discoveredWallets`. `connect()`/`switchWallet()` are unchanged and stay zero-friction when there's 0 or 1 wallet installed (the common case); `App.tsx` only shows the new `WalletPicker.tsx` overlay when `discoveredWallets.length > 1`, letting the user pick an extension by name/icon before signing in. `useWallet.ts` now tracks which specific provider actually signed the session (`activeProvider`) rather than always assuming `window.ethereum`, so the `accountsChanged` listener and future switches target the right extension. Verified with a scripted-browser (Playwright) test simulating two independently-announcing EIP-6963 wallets: confirmed the picker lists both by name, connecting via one produces that wallet's real address, and clicking "Switch wallet" with two installed re-opens the picker (rather than silently reusing the same extension) and correctly switches identity when the other is chosen.

**New this session (session 7, second wave) — Leaderboard (`LeaderboardScreen.tsx`, `server/src/leaderboardRepo.ts`):** raised directly by the user ("people love that in games — most wins, highest win-rate, most Coins earned"). Three tabs — Most Wins, Win Rate, Coins Earned — each a top-20 board plus a "you're ranked #N" line for whoever's connected. Deliberately **no new schema or write path**: every number is a read-side aggregate `group by`/`rank() over (...)` query straight off the existing `coin_transactions` audit log (session 5's `match_win`/`match_loss`/`match_draw` reasons for the first two, every positive credit — match rewards, welcome bonus, daily rewards, quests — for "Coins Earned"), so it can never drift out of sync with the ledger it's reading and needed zero migration. "Coins Earned" is lifetime gross earned, not current balance — spending on packs/crafting doesn't drop your rank, which matters since engaged players spend the most. Win Rate gates entry at `MIN_GAMES_FOR_WIN_RATE` (5, first-pass number) so a 1-0 record can't top a 40-10 veteran; below the threshold, `getMyWinRate` still reports real games/wins with `rank: null` so the UI can show "play N more matches to qualify" instead of just hiding the player. The board itself is public — reachable from the main menu with no wallet at all (same "no login wall to look" principle as Packs' `GET /api/packs`) — only the "you're ranked #N" line needs a token, and that's soft: `GET /api/leaderboard/*` never 401s, an absent/invalid token just omits `mine`. Only Play Online counts (Play vs AI never writes `coin_transactions`, same server-validation reasoning it's excluded from Coins/quests everywhere else). Verified: `db.test.ts`/`api.test.ts` cover ranking order, the zero-wins/below-threshold omission cases, and lifetime-earned surviving a pack purchase (78/78 server tests green against real Postgres), plus a scripted-browser Playwright run seeding real match history for two accounts and confirming all three tabs rank correctly (including a below-#1 "you're ranked #2" case) both logged out (public, "connect to see where you rank") and logged in.

**New this session (session 5) — a real Coins-earn loop for Play Online (`OnlineMatch.tsx`, `useOnlineMatch.ts`):** winning (or losing, or drawing) an online match now actually pays Coins — see Section 5 for the server side. `connect()` sends the wallet's session token on `findMatch` (anonymous play still works exactly as before if there's no wallet connected); when the server's new `matchReward` message arrives, a small "+N Coins" line renders in the app bar next to Leave. Play vs AI still awards nothing — deliberately, since it's entirely client-side with no server-validated outcome, so trusting a client-reported win would be an open cheat vector; solving that is out of scope, not an oversight.

**Sound + a fuller VFX pass (session 14):** the user asked directly for the game to feel "more fun," with sound effects and visual effects, while Pixa image-generation credits (branding work) were regenerating. Sound was a real gap until now (session 5's polish pass explicitly skipped it — "no audio assets exist or are being sourced"); solved without ever sourcing an asset: `sound.ts` is a small Web Audio synth engine — every effect (card play, attack, hit, death, heal, end turn, "your turn," error, win/lose/draw, Market Event, pack/quest/craft rewards, UI clicks) is oscillators shaped with a gain envelope at play-time, not a loaded file. Deliberate: zero licensing/sourcing work, zero bundle cost, and square/triangle-wave blips are exactly the "retro arcade terminal" sound the existing visual identity (session 8) already wants. Mute state persists to `localStorage`, surfaced via a new `MuteToggle.tsx` (🔊/🔇) in every app-bar that plays sound (main menu, both match views).
- **How match sounds are triggered** (`useMatchSounds.ts`): purely by diffing successive `MatchState` snapshots against the previous one — no engine changes. `state.log` entries are free-form text (`engine/src/types.ts`'s `LogEntry`), not typed events, so this pattern-matches the handful of stable phrasings `pushLog()` actually uses (`" dies."`, `" attacks "`/`" trades with "`, `" heals "`, `" plays "`, and the Market Events' distinct all-caps names) rather than requiring a structured event system — a wording drift would just mean a sound silently doesn't fire, never a crash, since only text is read. Same "diff-against-a-ref, one-shot-per-real-transition" shape `CardFace.tsx`/`PlayerHeader.tsx` already used for hit-flash (session 5). Win/lose/draw and "it's your turn now" are separate direct state diffs (`state.winner`, `state.activePlayer`), not log-parsed.
- **VFX added alongside:** floating damage/heal number popups on `CardFace`/`PlayerHeader` (red `-N`/green `+N`, pop-and-fade, same one-shot health-diff mechanism now also computing the delta instead of just detecting *that* it changed) — the single highest-value "juice" addition per typical game-feel wisdom. A brief full-table screen flash on any Market Event (`useMatchSounds` also returns a `marketEventFlash` counter for this). A pulsing glow on the Volatility meter once it's ≥8/10, telegraphing "an event is close." Deliberately not attempted this session: an attack-lunge/motion animation (needs to know which on-screen slot attacked — cheap for the local player's own attacks, not for the opponent's/bot's without either deeper log-slot-parsing or an engine change, so left as a real gap rather than a half-solution — see roadmap).
- **Reward sounds** wired into `PacksScreen.tsx` (a soft tick per common card, a brighter shimmer for Rare+/foil pulls — reusing spec.md Section 17's "signature moment" framing), `QuestsScreen.tsx` (daily claim, quest claim), `CraftingScreen.tsx` (Dust earned, card crafted), and `OnlineMatch.tsx` (the existing `matchReward` banner).
- Verified with a scripted-browser (Playwright) run patching `OscillatorNode.prototype.start` to count real sound triggers without needing actual audio playback verification: confirmed sounds fire on card-play attempts, End Turn, and combat (1 → 3 → 12 cumulative across one match), confirmed damage popups render with the correct delta (a real `-4` and `-1` captured on screen), and confirmed the mute toggle flips state correctly. Client type-checks and builds clean.
- **Correction, found session 15:** that verification's sound counts were real, but almost entirely from the *direct* UI-click sounds (select/card-play/End Turn, called straight from the click handlers) — the log-diffed engine sounds (attack/death/heal/Market Event/win-lose-draw) and the attack-lunge groundwork were silently broken for Play vs AI the whole time. See below.

**Attack-lunge animation + a real bug fix (session 15):** the user, after seeing the sound/VFX pass, asked for two more things directly: "when you attack the cards actually move," and a win/lose popup with confetti. Both landed — but building the lunge surfaced a real bug in session 14's work.
- **The lunge** (`useAttackAnimations.ts`) turned out solvable for *both* sides without the engine change session 14 assumed it would need: rather than parsing the log for "which slot attacked" (unreliable — the face-attack log line doesn't even include a slot number), it diffs each board creature's existing `hasAttackedThisTurn` flag (already tracked by the engine, already read by `BoardRow.tsx` for the `dimmed` styling) against the previous `MatchState` snapshot — false→true is an attack, for the local player, the remote opponent, *and* the bot uniformly, since it reads engine state rather than "who dispatched this." `MatchView.tsx` passes a physical `attackDirection` ("up" for my board, "down" for the opponent's, since MatchView always renders the opponent's row above mine) so the lunge always reads as "toward the enemy" regardless of whose creature it is. One real interaction handled explicitly: a creature-vs-creature trade damages both creatures in one resolution (`combat.ts`), so the attacker can be simultaneously "just hit" and "just attacked" — since CSS's `animation` shorthand doesn't merge across separately-applied classes (last one in the cascade wins outright, not both), a compound `.card-face--hit.card-face--lunge-{up,down}` rule plays both keyframe animations together.
- **The bug this surfaced:** `useMatch.ts` (Play vs AI) mutates its `MatchState` in place and forces re-renders via an unrelated `version` counter — the `state` object reference *never changes*. Session 14's `useMatchSounds` and this session's first `useAttackAnimations` draft both used `useEffect(..., [state])`, which only fires once, on mount, when the dependency is a reference that's never actually different — so for Play vs AI specifically (the far more commonly used mode), every log-diffed sound and the entire lunge mechanism were silently inert past the first render. `useOnlineMatch.ts` hands back a genuinely fresh object every server message, so Play Online was never affected — which is exactly why session 14's scripted verification (which only exercises Play vs AI) *looked* like it passed: the direct-click UI sounds it was actually measuring don't depend on `state` at all. Fixed by keying both hooks' effects on primitives read off `state` (`log.length`, `winner`, `activePlayer`) instead of the object itself — the same pattern `CardFace.tsx`/`PlayerHeader.tsx` already used correctly for hit-flash, which is why *that* mechanism was never broken. Caught only because this session's verification specifically polled for the lunge class through a real multi-turn match instead of trusting a single narrow-window check — worth remembering: a scripted check that passes by observing the wrong signal is worse than no check, because it looks like coverage.
- **The result overlay** (`MatchResultOverlay.tsx`): a dismissible full-screen card on match end, with a "ticker tape parade" — thin falling colored strips in the brand palette, not round confetti dots, since that's a real Wall Street victory tradition and ties directly into `branding.md`'s "the floor is the battlefield" identity. Deliberately no strips on a loss — celebrating that would read as mocking the player. Dismissing (click the card's "Continue" button or the backdrop) doesn't end the match view — the board stays visible underneath for review, same as before this overlay existed.
- Verified with a scripted-browser run that specifically polled for the lunge class across a full multi-turn match (rather than one narrow post-click window, the mistake in session 14's check) — confirmed it fires from the bot's own attacks as well as the local player's, confirmed the result overlay renders and dismisses correctly (`YOU LOSE` in red, no strips, captured on screen across several real match losses against the improved bot AI), and confirmed the win/confetti CSS renders correctly via a static mockup using the exact same classes (a real match win proved hard to script against session 5's smarter bot within a reasonable turn budget — the confetti code path itself is simple, type-checked, and visually confirmed independently). Client type-checks and builds clean.

**New this session (session 17) — a real card-frame redesign + a full mobile pass (`CardFace.tsx`, `factionTicker.ts`, `styles.css`):** the roadmap's long-flagged "still open: a fuller mobile pass, and a real card-frame/board redesign beyond the palette re-skin" (item 9), tackled as one of five parallel worktree tasks this session (see Section 8). Replaced the flat re-colored rectangle with a real Hearthstone-style frame, achievable purely in CSS/SVG since no art pipeline exists: rarity (`rarityColor.ts`) now drives both the outer border and a diamond gem at the name-plate seam; a new faction "ticker" badge (`factionTicker.ts` — DOG/FRG/DGN/CBR/BLD/NRM/NTR) plus a top color stripe stand in for a faction icon, typographic rather than inventing a new visual style; a faction-tinted radial-glow "portrait" band fills the art void over the existing scanline texture; Cost/Attack are hexagon gems, Health a radial-gradient circle gem, name in its own plated banner, keywords/text in an inset box. Every existing hook (hit-flash, damage/heal popups, foil shimmer, keyword tooltips, lunge animation classes) verified still working. Mobile: audited main menu/match board/landing/Packs/Collection at 375/390/428/768px and caught a real bug — `.app-bar` had no `flex-wrap`, so once title+subtitle+actions didn't fit one row, the *title text itself* shrank into a mid-word wrap ("CRYPTO"/"CLASH") instead of the action buttons wrapping to their own row; fixed by moving `flex-wrap: wrap` + `white-space: nowrap` on the title into the base rule instead of only the ≤400px one. Verified with a scripted Playwright pass (20+ screenshots) and an automated `scrollWidth`/`clientWidth` overflow check at all four widths — zero horizontal overflow anywhere, before-and-after on the app-bar fix.

**New this session (session 17) — opponent hand redaction (`OpponentHandRow.tsx`):** the client-side half of the Secrets/hand information-leak fix — see Section 5 for the server-side redaction this depends on. A real face-down, count-only row (`.card-face--back`, a diagonal-stripe card back with a faint "CC" watermark, using existing dark-trading-floor tokens) replaces what would previously have been an ability to render the opponent's real hand contents, now that the server never sends them across the wire at all.

**New this session (session 17) — Achievements screen (`AchievementsScreen.tsx`) and a Ranked tab (`LeaderboardScreen.tsx`) and a Weekly reward card (`QuestsScreen.tsx`) and an Event banner (`EventBanner.tsx`):** the client side of session 17's Coins-loop-remainder work — see Section 4 for the full server-side design (`achievementsRepo.ts`/`rankRepo.ts`/`weeklyRepo.ts`/`eventsRepo.ts`). Achievements follows the Quests screen's card pattern for permanent, one-time claims (win 10/50 matches, open 25 packs, craft a Legendary, hit a 5-game win streak). Ranked is a new tab alongside Most Wins/Win Rate/Coins Earned showing a Bronze→Diamond tier derived from a real points ledger — display/progression only, no matchmaking pairing built here. Weekly reward is a card next to the existing Daily one, same 4-week escalating/cycling shape. Event banner is public (no wallet needed, same "no login wall to look" principle as the Leaderboard) and surfaces an active Coins-multiplier event if one's configured. `tsc`/`vite build` clean; server-side got the deep integration-test coverage this session (Section 4), client verification here was a live-server smoke check (`curl` against the new endpoints) rather than a full scripted-Playwright pass — flagged by the session as the one thing scoped down given everything else landing at once.

**New this session (session 16) — an armed-Secrets badge (`PlayerHeader.tsx`):** a small follow-up to the balance/mechanics pass (Section 2) — `PlayerState.secrets` existed with nothing surfacing it in the UI, so neither player had a visual cue that a Secret was armed on either side. Shows "🔒 N" next to the Deck count whenever `secrets.length > 0`, on both the local and opponent header. Client `tsc`/`vite build` both clean; not visually verified in a live browser this pass (the Chrome extension wasn't connected).

**Client polish pass (session 5, roadmap step 8 — partially done):** `CardFace.tsx` and `PlayerHeader.tsx` now diff each render's health/HP against the previous one (a `useRef`/`useEffect` pair) to fire a one-shot shake-plus-red-flash animation exactly when a hit lands, instead of the number just silently changing. The five keyword badges (Rush/Guard/Stealth/Burn/HODL) now carry a hover tooltip with a one-sentence explanation pulled from `batlleSpec.md` Section 11 (`keywordInfo.ts`) rather than an unexplained all-caps word. Added hover/lift states, a glowing "Your move" turn indicator, and a pulsing End Turn button when it's actionable. A new `@media (max-width: 400px)` block fixes app-bar/board/hand/player-header overflow at small (~375px) viewport widths. Deliberately not done: sound (no audio assets exist or are being sourced) and any art-direction change (flagged below as a decision for the user, not something to decide unilaterally).

Both of the above were built as parallel, isolated `git worktree` tasks (alongside the bot AI work in Section 2) run concurrently in one session, then reviewed and merged by hand — worth knowing if a future session sees worktree branches or `.claude/worktrees/` and wonders where they came from.

**New this session (session 19) — a first-match Tutorial mode (`tutorial/`):** landed via `docs/product/TUTORIAL_SPEC.md`/`ENG_HANDOFF_TUTORIAL.md` (a product-to-eng handoff doc pair, dated 2026-09-09, checked into the repo alongside the feature — first time this project's docs have carried that split) and shipped as PR #1, implemented outside this document's normal session-by-session narration (no prior STATUS.md entry describes it being built) and picked up here purely for review. Client-only, no wallet: a fixed Normies starter vs. a scripted Doggos "coach" bot (`scriptedBot.ts` — hand-authored per-turn plans, not the real greedy AI), six blocking coach beats (`beats.ts`'s `gateIntent`/`coachFor`/`spotlightFor`) that teach Energy/play → summoning sickness → Rush/face → Guard → face-open → the full loop, each gating which `Intent`s are legal until the taught action actually happens. Reuses `MatchView` unmodified in spirit (a `tutorialMode`/`spotlight` prop pair layers on top, same pattern session 5's deck-picker used). Entry is a one-time auto-offer modal (`shouldOfferTutorial()`, gated on `localStorage` completed/skipped flags) plus — after this session's review fix — a permanently-reachable "Tutorial" main-menu button that no longer disappears once the flags are set.
- **Code review (`/code-review`) caught 8 real bugs before merge**, all fixed this session: beat 3 ("ramp + attack with Rush") could be passed by attacking face with an old non-Rush creature — the gate now checks `attackerSlot`'s actual Rush keyword; a match ending before all 6 beats completed left the player on a dead-end result screen with no finish path (`showTutorialResult` no longer requires `beatsComplete`); the injected Rush card (`ensureRushInHand`) could silently fail to appear because the effect that injects it didn't always trigger a re-render; the header "Menu" button abandoned the tutorial with no confirmation (unlike the adjacent "Skip tutorial" button); the hand-spotlight highlight hardcoded the literal card id `"steady_hand"` instead of deriving it from state; `scriptedBot.ts` duplicated `engine/src/bot.ts`'s private `tryIntent` helper instead of importing a shared, now-exported one; plus one dead-code branch. Verified with `tsc --noEmit`/`vitest` across all packages (56 engine + 21 server tests green) and a headless script exercising the real engine + `beats.ts` gate logic through beats 1-4 (confirmed the Rush-gating fix rejects a non-Rush face attack and accepts the real Rush creature). Squash-merged into `main`.
- **Also filed by the handoff, not yet fixed** (`ENG_HANDOFF_TUTORIAL.md`'s "also file, do not block tutorial" list — real gaps in the *existing*, non-tutorial client, surfaced during the handoff author's own live-client walkthrough): the Online queue screen has no timeout/leave/empty-queue copy while solo-waiting; the match view's internal scrollbar can clip headers/controls and the Match Log can intercept the Menu button until dismissed; a repro-needed report that a non-Rush creature's attack "sometimes didn't visibly resolve" (Pup Scout, unconfirmed); and a product-flagged IA gap where Collection/Packs/Crafting are fully hidden from the main menu pre-wallet-connect rather than shown greyed-out with a "Connect wallet to collect" affordance. None of these four are scheduled yet.

**Not yet built:** Coins from anything but a match result or the one-time welcome bonus (quests/dailies/achievements — spec.md Section 21), First Edition/Serial actually being granted (foils now are — see Section 4/session 6 — but the collection screen and schema are still ready-but-unused for those two), sound, art direction beyond the current minimal dark theme. See Section 7.

---

# 4. Accounts, Decks & Collection Backend (`server/`)

Started session 3 (accounts + decks) and extended this session (session 4) with `architecture.md` Section 6's rarity/collectible layer — editions + instances, no `card_templates` table (see Section 6's deviation note in `architecture.md`, same reasoning as `decks.cards`: gameplay identity stays in `CARD_POOL`, nothing duplicates it into Postgres). Lives in the same Railway service as the match server (same "Game Backend" box the architecture doc's own diagram already draws), exposed as `/api/*` REST routes alongside the existing WebSocket endpoint.

- **Schema**, applied via `npm run migrate --workspace=server` (a small hand-rolled runner — no ORM/framework):
  - `0001_accounts_and_decks.sql`: `accounts` (wallet address, lowercased, unique) and `decks` (account-scoped, `cards` as a JSON array, cascade-deletes with the account).
  - `0002_editions_and_instances.sql`: `card_editions` (`template_id` text — a `CARD_POOL` key, not an FK — `edition_type` standard/first_edition/legendary/genesis, unique per template+type) and `card_instances` (`owner_id` → `accounts`, `edition_id` → `card_editions`, both cascade-delete, plus nullable `serial_number`/`onchain_token_id` and an `is_foil` flag for when packs/minting actually populate them).
  - `0003_coins_and_packs.sql` (new this session): `accounts.coins_balance` (a cached running total), `coin_transactions` (append-only audit log — positive amount = earn, negative = spend, both cascade-delete with the account), `pack_openings` (one row per pack pulled: `pack_type`, `coins_spent`, `cards` (the template ids pulled, in reveal order), and the `mulberry32` `seed` the roll used — reproducible and auditable, same determinism-by-construction story as match replays).
- **Auth** (`server/src/auth.ts`): Sign-In with Ethereum — nonce issue/consume (one-time, 5-minute TTL, in-memory), SIWE message verification (`siwe` package) checked against a configured domain (replay/cross-app-reuse protection), session as a signed JWT (`jose`). Fully tested with real ECDSA signing via `ethers.Wallet` — no mocking of the crypto itself. `findOrCreateAccount` (`accounts.ts`) now also returns `isNew` (via Postgres's `xmax = 0` upsert trick) — the welcome-bonus grant's one-time-only gate, see Coins below.
- **Collection** (`server/src/collectionRepo.ts`): `grantStartingCollection` gives every account `MAX_COPIES_PER_CARD` (3) standard-edition instances of every **Common**-rarity template (narrowed this session from "every non-token template" — see Coins/Packs below for why) — called on every sign-in, idempotent (tops up rather than duplicates, so it also back-fills any Common template added to the pool after an account's first sign-in). `getCollectionCounts` aggregates owned copies per template id; `validateOwnership` checks a prospective deck's card counts against them. New this session: `grantCardInstances` — adds exact template ids as new instances (duplicates included, no top-up capping), the shared primitive packs use to actually grant a pull; takes a `PoolClient` so it composes into packsRepo's own transaction.
- **Coins & Packs** (`server/src/coinsRepo.ts`, `server/src/packsRepo.ts`): `grantWelcomeBonus` credits a new account (gated on `isNew`, not idempotency-checked separately) a flat `WELCOME_BONUS_COINS` (3,000 — a placeholder number, not tuned; there's still no real Coins *earn* loop, since matches aren't tied to authenticated accounts yet). `openPack` is one atomic transaction: locks the account's balance row (`for update`), checks affordability, rolls `PACK_DEFINITIONS.standard`'s 5 cards against a fresh seed via the engine's own `mulberry32` (exported from `engine/src/index.ts`), debits Coins, grants the instances, and logs the pull — a mid-way failure can never charge without granting or vice versa. Rolling (`rollPackCards`) is a pure function of `(packType, seed)`, tested independently of the DB/coin side.
  - **Session 6 — real odds design, replacing the "everything's basically Common" placeholder:** `NORMAL_ODDS` (Common 65% / Uncommon 25% / Rare 8% / Epic 1.8% / Legendary 0.2%) for the first 4 slots, `LAST_SLOT_ODDS` (Uncommon 52% / Rare 33% / Epic 12% / Legendary 3%) for the 5th so every pack still guarantees at least one Uncommon+. Against today's 63-template pool this gives ~0.038 expected Legendary pulls/pack (~1 every 26 packs) and ~0.23 expected Epic+/pack — a real long-tail chase curve, though still a first pass to revisit against actual telemetry, not final tuned numbers. A new `PACK_ELIGIBLE_RARITIES` set (Common–Legendary only) is checked at module load against both odds tables — Mythic and Genesis throw if anyone ever adds a weight for them here, enforcing spec.md Section 16's "permanently capped" promise for Genesis in code, not just in a comment.
  - **Session 6 — foils, a second/independent collectible axis:** every pack card also rolls a flat, rarity-independent `FOIL_CHANCE` (8%) — any template, Common through Legendary, can come out foil, closer to a Pokémon "shiny" than a value multiplier stacked on rarity. Stored on `card_instances.is_foil` (the migration 0002 column existed but was unused until now). `grantCardInstances` (`collectionRepo.ts`) now takes `PackCard[]` (`{templateId, isFoil}`) instead of bare template ids, inserting `is_foil` per instance via two zipped `unnest()` calls in one `INSERT ... SELECT` (lockstep since Postgres 10 — not a cross join). `getCollectionSummary` returns owned counts *and* foil counts together in one query (a conditional aggregate — `count(...) filter (where is_foil)` — caught and fixed in this session's code-review pass, which had originally split this into two separate round-trip queries). `GET /api/collection` now returns `{ owned, foils }` (additive — existing `owned`-only consumers are unaffected). First Edition and Genesis/Mythic are deliberately *not* pack outcomes at all (see the `PACK_ELIGIBLE_RARITIES` note above and spec.md Section 17) — both need their own grant mechanism (a time-boxed print-run flag; an event/achievement path) that doesn't exist yet.
- **Crafting** (`server/src/craftingRepo.ts`, new this session): a third ledger alongside Coins — `accounts.dust_balance` + append-only `dust_transactions` (`0004_dust_and_crafting.sql`), fully separate from Coins, never interchangeable. `disenchantCards` converts `count` owned copies of a template into Dust (non-foil instances consumed first via `order by is_foil asc`, protecting a player's foil pulls from a bulk disenchant by default); `craftCard` spends Dust to grant one new standard, non-foil instance via the existing `grantCardInstances`. Both lock the `accounts` row first (`select ... for update`) before touching `card_instances`, serializing concurrent disenchant/craft calls on the same account the same way `openPack` serializes concurrent pack opens. `CRAFT_ELIGIBLE_RARITIES` (Uncommon–Legendary) excludes Common — allowing it would let a player farm Dust for free, since `grantStartingCollection` re-tops every account's Commons on every sign-in — and excludes Mythic/Genesis for the same permanently-capped-supply reason packs do. Dust values (Uncommon 10/70, Rare 20/100, Epic 100/400, Legendary 400/1600 — disenchant/craft) are anchored to Hearthstone's long-tested disenchant economy, a first design pass per spec.md Section 18. `getCraftRates()` is pure/no-DB, served over `GET /api/craft/rates` so the client never hardcodes a second copy of these numbers.
- **API** (`server/src/httpApi.ts`): `GET/POST /api/auth/nonce|verify`, `GET /api/collection`, `GET/POST /api/decks`, `PUT/DELETE /api/decks/:id`, `GET /api/coins`, `GET /api/packs` (public), `POST /api/packs/open` (`402` if under-funded, `400` for an unknown pack type), plus new this session `GET /api/craft/rates` (public), `GET /api/dust` (authenticated), `POST /api/craft/disenchant` (`409` if under-owned), `POST /api/craft/craft` (`402` if under-funded, `400` for an uncraftable template). Every deck write re-validates against both `validateDeck()` (pool-legality) and `validateOwnership()` server-side, `422` on either failing. Cross-account deck access returns `404` (not `403`), so existence isn't leaked.
- **Tests**: `auth.test.ts` (pure, no DB). `db.test.ts` + `api.test.ts` (real Postgres integration) — coins (welcome-bonus grant/idempotency-via-`isNew`/per-account scoping) and packs (deterministic rolling, a full open debiting Coins and granting instances, insufficient-Coins rejection charging/granting nothing, `grantCardInstances` preserving duplicates past the 3-copy cap) coverage, plus the existing deck-CRUD fixtures reworked to an all-Common 30-card deck now that the starting grant no longer covers Rare+. Both still `describe.skip` without `DATABASE_URL`.
  - **Session 6 additions:** a 500-seed sweep asserting `rollPackCards` never produces a Mythic/Genesis card (the hard exclusion, not just an unlikely outcome to not hit), a 500-seed sweep asserting the observed foil rate lands strictly between 0% and 20% (a real, nonzero-but-minority rate, not a tight statistical bound), `grantCardInstances`/`getCollectionSummary` coverage confirming `is_foil` survives per-instance through a duplicate grant, and a full crafting suite (disenchant crediting the right Dust and removing instances, foil-protection ordering, insufficient-copies/-Dust rejections charging/granting nothing, the Common/token/unknown-template guard rail, and an HTTP-level round trip disenchanting then crafting a different card). Ran and passed in full (58/58) against a real local Postgres (Docker `postgres:16-alpine`, migrations applied fresh) this session, plus full scripted-browser (Playwright) runs through wallet-connect → pack-open → foil reveal → Collection screen, and separately through granting duplicates → Crafting screen → disenchant → craft → Collection screen, per Section 3.

**Session 7 — Daily rewards + Quests (`dailyRepo.ts`, `questsRepo.ts`, migration `0005`):** the second half of the Coins-earn loop (STATUS.md roadmap step 10; spec.md Section 21). Both a fourth Coins source (via `applyCoinDeltaOnClient`, same ledger primitive as everything else) and a reason to come back and play *today*, on top of the one-time welcome bonus and match results.
- **Daily login** (`dailyRepo.ts`): a 7-day escalating reward table (`DAILY_REWARDS`, 50→60→75→90→110→140→250), keyed by `accounts.last_daily_claim_day`/`accounts.daily_streak`. Claiming locks the account row, checks it hasn't already claimed *today* (UTC), then either extends yesterday's streak by one or resets to 1 if a day was missed — the streak isn't capped at 7, it cycles (day 8 pays day 1's rate again), so a long streak keeps paying rather than plateauing. Both "today" and the stored claim day are plain `YYYY-MM-DD` UTC strings (a `text` column, not Postgres `date`) — `pg` parses `date` into a JS `Date` object by default, which would reintroduce exactly the local/UTC ambiguity this is trying to avoid.
- **Quests** (`questsRepo.ts`): a fixed, code-defined list (`QUEST_DEFS` — Play 1/Play 3/Win 1 match), same "no config table, gameplay-adjacent identity lives in code" reasoning as `CARD_POOL`. `quest_progress` (migration `0005`) is one row per `(account, quest, day)`; `recordQuestProgress(pool, accountId, track)` upserts +1 progress (capped at goal) for every quest on that track, `claimQuest` atomically flips `claimed` false→true only when progress already meets the goal (a single `update ... where ... returning` is the whole compare-and-set, no extra lock needed) before crediting Coins.
- **Match hook** (`matchRoom.ts`): right where the existing Coins match-reward fires (the `null → non-null` winner transition), each participant with a real account now also gets `recordQuestProgress(pool, accountId, "play")` and, for the winner, `"win"` — best-effort, same as the Coins award, so a tracking failure can't crash the match.
- **API** (`httpApi.ts`): `GET /api/daily`, `POST /api/daily/claim` (`409` if already claimed today), `GET /api/quests`, `POST /api/quests/:id/claim` (`400` unknown quest, `409` incomplete or already claimed).
- **Tests:** `db.test.ts` covers first-claim streak=1, same-day double-claim rejection, streak extension vs. reset-after-a-gap (simulated by writing `last_daily_claim_day` directly), the 7-day cycle wrapping, quest progress capping at goal per-track, and claim-once-then-reject-a-repeat. `server.test.ts`'s existing real-WS match-to-conclusion test now also polls (`recordQuestProgress` is fire-and-forget, not part of the `matchReward` message, so there's no message to await) for both participants' "play" quest advancing and the winner's "win" quest advancing. `api.test.ts` covers the HTTP-level 409/400s and the no-auth-header 401s. All run against a real local Postgres (Docker `postgres:16-alpine`) this session — 72/72 server tests green.

**New this session (session 17) — Weekly rewards, Achievements, Ranked progression, and Events (`weeklyRepo.ts`, `achievementsRepo.ts`, `rankRepo.ts`, `eventsRepo.ts`, migration `0007`):** closes out spec.md Section 21's Coins-earn loop (STATUS roadmap item #10's last four sources), built as one of five parallel worktree tasks this session — see Section 8 for the merge process (three of these four systems hook into `matchRoom.ts`'s existing match-result path alongside session 16's Secrets/redaction work landing the same session, so this required deliberate manual conflict resolution across three overlapping branches, not a blind auto-merge).
- **Weekly rewards** (`weeklyRepo.ts`): exact mirror of `dailyRepo.ts`'s lock→check→credit shape, but a 4-week escalating table keyed by ISO week instead of a UTC day.
- **Achievements** (`achievementsRepo.ts`): permanent, one-time claims (`accounts.current_win_streak` is new, tracked alongside) — win 10/50 matches, open 25 packs (paid *and* free/referral, since it hooks `packsRepo.ts`'s shared `rollGrantAndLog`), craft a Legendary. Hooked into the same real, server-validated events Coins/quests already trust.
- **Ranked progression** (`rankRepo.ts`): a separate append-only `rank_points_transactions` ledger (kept apart from `coin_transactions` since a loss can cost rank points while still earning some Coins) mapped to Bronze/Silver/Gold/Platinum/Diamond tiers. Deliberately display/progression only — matchmaking is still FIFO, no skill-based pairing was built here. No season/reset design yet — points just accumulate forever, a real product decision still open.
- **Events** (`eventsRepo.ts`): scoped down hard and deliberately — a `coin_multiplier` DB-row config applied only to Play Online match rewards (not daily/weekly/quest/achievement claims — a documented scope cut, mechanically easy to extend later), gated by a crude `ADMIN_SECRET`-header route since no real admin-auth system exists yet. No first event's actual content decided — left as an unbuilt example on purpose.
- A real bug the new tests caught: `recordAchievementProgress`'s `least($3, $4)` on two untyped bare params made Postgres infer `text`, breaking the moment any pack/craft/match hook ran it — fixed with explicit `::int` casts.
- Every new number here (weekly amounts, achievement rewards, rank point deltas/tier thresholds, the example 2× event multiplier) is a first-pass placeholder, same convention as every other economy constant in this codebase.
- **Tests:** 34 new server tests (28 from the originating task plus a few more surfaced during the session's merge-verification pass), including a full WS-level match-to-conclusion test confirming the achievement/rank hooks fire for real — 122/122 server tests green against real local Postgres, migrations `0001`–`0007` applied cleanly. **Applied to production** (session 18) via the same `railway connect Postgres --tunnel-only` step every migration since `0002` has used — confirmed via `_migrations`.

**New this session (session 9) — Referrals (`referralsRepo.ts`, migration `0006`):** a viral-growth invite loop, added directly at the user's request ("some sort of social invite feature eg free packs or free card to get the viral spread going"). Both sides get a free Standard Pack: the *referred* player's is granted immediately at their first sign-in (they already took the one real action that matters — connect + sign); the *referrer*'s is granted only once the referred account completes its first Play Online match (`matchRoom.ts`'s post-match hook, the same server-validated-event bar Coins/quests already use), so simply creating a wallet doesn't pay out on its own.
- Every account gets a lazily-generated `referral_code` (`accounts.referral_code`, only created on first request — most accounts never share theirs). A `referrals` row (`referrer_id`, `referred_id` unique, `status` pending→rewarded) is created once, at signup, never retroactively for an existing account. `rewardReferrerIfPending`'s `update ... where status = 'pending' returning` is the whole compare-and-set — fires exactly once per referral.
- Free packs reuse the exact same roll/grant/audit-log pipeline as a paid pack open — `packsRepo.ts`'s `openPack` was refactored to extract a shared `rollGrantAndLog(client, accountId, packType, coinsSpent)` so a free grant (`coinsSpent: 0`) is a real, reproducible, auditable `pack_openings` row, not a special case.
- **Known, documented gap:** two wallets under one attacker's control can refer each other and play one match together to farm one free pack each — bounded by the same "needs a live opponent" friction that already caps Play Online Coins farming, not a new hole this feature opens. Not worth a rate limiter for a feature with zero real users yet; flagged in the code, not silently ignored.
- **Client** (`ReferralScreen.tsx`, reachable via "Invite Friends" in the main menu): shows the shareable link + a Copy button, and live Invited/Free-Packs-Earned/Awaiting-First-Match counts. `referral.ts` captures a `?ref=CODE` URL param into `localStorage` at app boot (`main.tsx`) and `useWallet.ts`'s `connect()` sends it (one-shot — consumed regardless of outcome) on the next sign-in.
- **API:** `GET /api/referral` (authenticated — code + stats); `POST /api/auth/verify` gained an optional `referralCode` field, applied only when `Account.isNew` (best-effort — a garbled/unknown code never blocks sign-in).
- **Tests:** `db.test.ts` covers code generation/stability, signup granting a free pack + a pending referral row, unknown-code/self-referral no-ops, the reward flipping exactly once, and a no-op for an unrelated account. `api.test.ts` covers the HTTP-level flow and auth. `server.test.ts` gained a real end-to-end WS test: a referred account plays an actual match to a real conclusion and the referrer's stats flip to rewarded. 88/88 server tests green (real local Postgres). Also verified with a 3-wallet scripted-browser (Playwright) run: a referral link generated and followed by a second real signup, confirmed the referred account received 59 card instances (54 starting Commons + a 5-card free pack) and the referrer's screen updated from 0→1 pending immediately, then to 1 rewarded/0 pending after the reward fired.

**Done:** production DB provisioned and reachable on the deployed site (see Section 1); `0002_editions_and_instances.sql` through `0007_weekly_achievements_rank_events.sql` all applied to production and verified there (`coin_transactions`/`pack_openings`/`dust_transactions`/`quest_progress`/`referrals`/`achievement_progress`/`events`/`rank_points_transactions`/`accounts.{coins_balance,dust_balance,last_daily_claim_day,daily_streak,referral_code}` all confirmed present via `railway connect Postgres --tunnel-only` — production Postgres has no public host var, so this SSH-tunnel-then-migrate approach is how every migration since 0002 has reached it). The DB password printed by that tunnel command has still not been rotated (same "asked, user said leave it" call from session 4) — worth revisiting if that ever changes.

---

# 5. Multiplayer (`server/`)

Matches architecture.md Section 5: FIFO matchmaking, one `MatchState` per room held in server memory, intents validated against the socket that sent them, state broadcast to both players after every legal action, opponent-disconnect notification.

**New this session (session 5) — Coins for playing Play Online:** `findMatch` (`shared/src/index.ts`) gained an optional `token`; `createMatchServer.ts` resolves it via `verifySessionToken` (never throws — a missing/bad/expired token is just anonymous play, exactly as before) and stashes the result as `Session.accountId`. `MatchRoom.handleIntent` (`matchRoom.ts`) captures whether `state.winner` was already decided before applying an intent, and awards Coins exactly once, right at the `null → non-null` transition — `100` for a win, `25` for a loss, `50` for a draw (`coinsRepo.ts`'s `awardMatchResult`/`MATCH_WIN_COINS` etc., placeholder numbers with the same "not tuned economy design" caveat as `WELCOME_BONUS_COINS`), to whichever of the two sessions has a real `accountId` — silently skipped (no crash, no message) for anonymous sessions or when `DATABASE_URL` isn't configured. A new `matchReward` server message carries the amount and new balance back to the client. Play vs AI is deliberately excluded — it's entirely client-side with no server-validated outcome, so awarding Coins there would trust a client-reported win.

Verified with a real WS-level integration test (`server/test/server.test.ts`) that drives an actual match to a genuine engine-decided win over live sockets (a scripted "develop board, respect Guard, attack face" player reacting only to real server-broadcast state — not reaching into `MatchRoom` internals), both anonymous (confirms no `matchReward`, no crash) and authenticated against real Postgres (confirms both accounts get credited, winner's `coinsEarned` > loser's, and the reported balance matches a fresh `getBalance` read) — 46/46 server tests passing. Also verified visually end-to-end: a real scripted-wallet browser client playing a live online match against a raw-WebSocket scripted opponent, confirming the "+25 Coins" banner actually renders in `OnlineMatch.tsx` after a real loss.

**New this session (session 17) — reconnect-to-in-progress-match (`matchRoom.ts`, `createMatchServer.ts`, `shared/src/index.ts`, `useOnlineMatch.ts`):** closes the "no reconnect" known limitation below. A socket dropping unexpectedly no longer ends the match — the opponent is notified (`opponentDisconnected`, with the grace window), and a 45s grace timer (`RECONNECT_GRACE_MS`) holds the seat open before forfeiting. Two ways to prove "same seat, reconnecting": a per-seat `reconnectToken` handed out at `matchFound` (works for anonymous play too, no persistent identity needed), with a wallet-session-JWT fallback for authenticated players who lost the token client-side (e.g. a page reload). On reconnect the authoritative state (plus any reward that fired while disconnected) is resent. If the grace period lapses, `forfeit()` sets a real winner and does a normal `state` broadcast — the old separate `opponentLeft` "dead screen" message is no longer sent (kept in the protocol for backward compatibility only), since a forfeit now always has a real decided winner the client's existing win/lose overlay already knows how to show. An intentional "Leave" click skips the grace period entirely. Rooms stay reconnectable 30s past conclusion (`POST_MATCH_HOLD_MS`) too, so a disconnected player can still see the real final result/reward. Client: an unexpected close (not an intentional Leave) now triggers bounded auto-reconnect attempts with a "reconnecting…" banner instead of a frozen board.

**New this session (session 17) — Secrets/hand information-leak fix (`shared/src/index.ts`, `matchRoom.ts`, client's `OpponentHandRow.tsx`):** a real, previously-documented gap (session 16/`card-schema.md` §8.2) — `MatchState` was serialized once and sent identically to both sockets, so a player could inspect network traffic (or a modified client) and see the opponent's entire hand, deck order, and every armed Secret before it triggered. `serializeState` now takes a `viewerId` and redacts per-socket: the non-viewer's `hand`/`deck`/`secrets` come across as same-length arrays of a `HIDDEN_CARD_ID` placeholder — real counts survive (so the session-16 "🔒 N" badge still works) but identities never cross the wire. Engine internals untouched — purely an outbound serialization change. Verified with real-WS tests confirming each player's own hand round-trips correctly while the opponent's socket only ever sees placeholder counts, including a pigeonhole-guaranteed Secret-trigger case (owner's broadcast shows the real Secret id, opponent's broadcast of the same event shows only a count).
- **This session's reconnect and redaction work landed in parallel and both touch `matchRoom.ts`'s broadcast path** — merged and reconciled by hand (not auto-resolved) rather than accepting either branch wholesale; see Section 8 for the merge process. Full server suite (122/122, including both features' new tests running together) confirms the composition is correct, not just that each half works in isolation.

**Known limitations, not yet addressed:**
- Skill-based matchmaking — currently just "whoever's next in the queue" (the session-17 reconnect task's stretch goal, not attempted — reconnect was prioritized instead)
- State lives in one process's memory — fine at current scale, won't horizontally scale past one server instance without more work

---

# 6. Architecture Decisions Made Since `architecture.md` Was Written

`architecture.md` is the design doc; this section is the "here's what we actually did" reconciliation. (Also reflected in `architecture.md` Section 13 directly.)

- **Client:** web-first (React + Vite), not Unity — confirmed and shipped.
- **Battle engine language:** TypeScript — confirmed and proven in production, including the server running it live via `tsx` (see `server/README.md` for why `tsx` in production rather than a compiled `tsc` build — the workspace packages point `main` at raw `.ts` source, so a plain `node dist/index.js` would crash).
- **Hosting split:** Vercel (client, static) + Railway (match server, needs a persistent WebSocket process — Vercel can't host that). This is the practical resolution of the open question architecture.md Section 13 flagged.
- **Monorepo shape:** `engine` / `shared` (protocol) / `server` / `client` as npm workspaces. `shared` wasn't in the original architecture doc's package list — it exists because `MatchState` has `Set`-typed fields and an `rng` function that don't survive `JSON.stringify`, so both sides needed one place owning that serialization boundary.
- **Identity/auth: wallet-connect from day one, overriding Section 9's "email/social first, wallet later."** Deliberate call, not an oversight — Section 9's reasoning (protect non-crypto-native players from wallet friction at signup) assumed a broad launch audience; this project's actual go-to-market is crypto-native (token launch, Web3-community acquisition — see Section 0), where a wallet is *lower* friction than inventing a password. The one piece of Section 9's intent kept: no login wall anywhere — Play vs AI/Online need no wallet at all, it's asked for only when something needs to persist.
- **Custodial-wallet question (Section 13, "still open"): now moot for the current stage.** Nothing here needs a custodial wallet yet — deck-saving only needs an address to sign in with, no asset custody. Revisit once minting/marketplace (Section 8) are actually being built.

---

# 7. What's Explicitly Not Started

From spec.md Section 34's "Later" list — this is now the **main line of work**, not a someday-list, per the direction in Section 0. Still true that none of it exists yet:

NFTs / Web3 Service, marketplace, staking/Vaults, tournaments, guilds, draft/sealed modes. **Wallet linking and accounts now exist** (Section 4) — struck from this list. **Rarity/editions/instances now exist** (Section 4) — struck from this list too. **Coins ledger, packs, and the collection screen now exist** (Section 3/4, session 5), pack odds got a real design pass plus a new foil mechanic, **crafting now exists** (Section 4, session 6), **daily rewards + quests now exist** (Section 3/4, session 7), and **weekly rewards, achievements, ranked progression, and events now all exist** (Section 3/4, session 17) — spec.md Section 21's Coins-earn loop is now fully built. Still not a finished, *tuned* loop: every reward number across the whole system (odds/foil/Dust/daily/weekly/quest/achievement/rank amounts) is a first design pass, not backed by real telemetry; ranked has no season/reset design; matchmaking is still FIFO, not skill-based.

The full system (rarity ladder, editions, serial numbers, duplicate-protection/crafting, packs, on-chain minting with Postgres as the deck-legality source of truth and the chain as the ownership source of truth) is already specified in spec.md Sections 12-21 and architecture.md Sections 6-8 — nothing here needs to be designed from scratch, only built, in the order architecture.md Section 12 lays out (off-chain data model → off-chain economy UI → only then Web3/minting/marketplace).

---

# 8. What's Next

No fixed roadmap beyond the immediate next step — this project is being driven conversationally, one milestone at a time, working autonomously and only surfacing genuine decisions.

**Session 19 (2026-09-10) — reviewed and merged the tutorial_v1 PR, then closed out item 16's session-17 audit backlog with a 4-way parallel swarm:** asked to review `github.com/josh04121998/CryptoClash` PR #1 (`feat/tutorial-v1`). See Section 3 for the full writeup — 8 real bugs found via `/code-review` and fixed (Rush-attack gating, a dead-end result screen, a silent card-injection re-render bug, an unconfirmed abandon button, a hardcoded spotlight card id, a duplicated bot helper, dead code), verified against real engine tests plus a headless script re-exercising the actual gate logic (no Chrome extension connected this session, so UI clicks weren't screenshotted — logic was verified directly against the engine instead), then squash-merged into `main`. Also logged four non-blocking gaps the tutorial's own handoff doc filed against the *existing* client (Online queue timeout/copy, Match Log intercepting Menu, an unconfirmed Pup Scout attack-resolution repro, and a pre-wallet IA gap hiding Collection/Packs/Crafting) — none scheduled yet, see Section 3.

**Then: the session-17 audit findings (roadmap item 16), all four landed** as a parallel `git worktree` agent swarm (same pattern sessions 5/17 used) plus hand-merging and verification:
1. **Server hardening** (`server/src/httpApi.ts`/`auth.ts`/`db.ts`/`createMatchServer.ts`) — `readJsonBody` now caps buffered request bodies at 1MB (413 past that, instead of buffering an attacker-sized body into the same process holding all live match state); `pruneExpiredNonces` (written in an earlier session, never called) is now wired onto a 60s `unref()`'d interval, cleared on server `close()`; a minimal in-memory per-IP rate limiter now covers `/api/auth/nonce` (30/min) and `/api/auth/verify` (20/min), preferring `x-forwarded-for` for Railway's proxy. **TLS certificate verification was implemented as an opt-in flag (`DATABASE_SSL_VERIFY=true`) and then, later this same session, actually tested live against production** (the agent that built it couldn't verify a real cert chain from its sandbox, so it correctly left it off by default) — flipping it on broke every DB-backed endpoint within about a minute (Railway's managed Postgres presents a self-signed cert; `self-signed certificate in certificate chain`), caught via a live `/api/leaderboard/wins` check, reverted immediately. Left `false` as the default; see the roadmap item 16 entry below and `db.ts`'s updated comment for the confirmed finding — real verified TLS here needs Railway's actual CA cert supplied explicitly, not just the flag.
2. **DB dedup + a real index** (`server/src/txHelper.ts`, 9 repo files, `server/migrations/0008_leaderboard_index.sql`) — the audit's "duplicated 6×" undercounted it; found and collapsed **11** identical connect/begin/commit/rollback/release sites across `achievementsRepo`/`coinsRepo`/`collectionRepo`/`craftingRepo` (×2)/`dailyRepo`/`packsRepo`/`questsRepo`/`referralsRepo` (×2)/`weeklyRepo` into one shared `withTransaction()` helper — pure refactor, verified behavior-identical against a real local Postgres (122/122 server tests green, including every refactored function's own integration test). Added a `coin_transactions(reason, account_id)` composite index matching the Leaderboard's actual query shape (`getTopWins`/`getMyWins` filter `reason = $2`, `getTopWinRate`/`getMyWinRate` filter `reason = any($n)`, both grouped by `account_id`) — `getTopCoinsEarned` filters on `amount > 0` instead, deliberately not targeted by this index.
3. **A real client test suite** (`client/vitest.config.ts`, `client/src/**/*.test.ts`) — the audit's "zero committed automated tests" gap, finally closed. 37 tests: 23 on `tutorial/beats.ts`'s `gateIntent`/`afterIntent`/`spotlightFor` (built against the real engine, not mocked — heaviest coverage on beat 3's just-fixed Rush-gating regression, verified to actually catch it by temporarily reverting the fix and confirming the test failed), 6 on `tutorialStorage.ts`'s fail-soft `localStorage` wrapper, 6 locking in the session-1 `apiUrl()` trailing-slash production bug, 2 on `useTutorialMatch`'s re-render fix via `renderHook`. `npm test --workspace=client` now exists as a real script alongside `engine`/`server`'s.
4. **A client ARIA pass** (22 component files) — the audit's "zero ARIA anywhere" gap. Icon-only buttons got real labels, the four modal/overlay patterns (`WalletPicker`, `TutorialOfferModal`, the tutorial pre-match card, `MatchResultOverlay`) got `role="dialog"`/`aria-modal`/`aria-labelledby`, live game-state updates (match errors, coach feedback, reward banners) got `aria-live="polite"`, form inputs got real `<label>` associations, and every board/hand card got a composed `aria-label` (name/cost/attack/health/keywords) since a screen reader previously got nothing meaningful off the card frame. Deliberately left as follow-ups, not attempted: focus-trapping/restoration for the four dialogs (none currently move focus on open), and a full alternate game-log/turn-announcer system for the board.

**The merge caught one real cross-branch bug the individual agents couldn't have seen**: merging the rate limiter (item 1) alongside the DB dedup branch (item 2) broke `server/test/api.test.ts` — that suite signs in dozens of accounts in quick succession from one IP, well within the new 20/min verify cap, which isn't the abuse pattern the limiter exists for. Fixed by bypassing the limiter when `NODE_ENV=test` (vitest sets this automatically; Railway never does). Full verification after every merge step, not just at the end, same discipline session 17's integration merge used: `tsc --noEmit` clean across all four packages, 56/56 engine tests, **122/122 server tests against a real local Postgres** (spun up fresh in Docker for this session, migrations 0001-0008 applied and confirmed idempotent on a second run), 37/37 new client tests, clean `vite build` for both server-adjacent and client packages. Merged into local `main` and pushed. **Migration `0008` applied to production** this session too (`railway connect Postgres --tunnel-only`, confirmed via `_migrations` and `pg_indexes` directly rather than trusting the migrate command's own output — same "verify, don't trust" norm session 18 established) — done only after asking the user directly, since it's a live production database write; both Vercel and Railway confirmed responding 200 afterward. **Roadmap item 16 is now fully closed** — see the roadmap list below.

**Session 18 (2026-09-09) — confirmed session 17's polish push is genuinely live, nothing built:** picked up from "where did we get to last session," asked to push + deploy session 17's work. Found `origin/main` already matched local `main` (`1a4903f`) — the push had actually already happened, despite session 17's note here saying otherwise. Opened a `railway connect Postgres --tunnel-only` tunnel and ran `npm run migrate --workspace=server` against production — it reported "Already up to date," and rather than trust that message blindly, queried the `_migrations` table directly plus checked for session 17's new tables (`achievement_progress`, `events`, `rank_points_transactions`) to confirm migration `0007` really was already applied. Confirmed both live deployments respond 200. **Net result: no new deploy action was actually needed — this session's real output was catching that STATUS.md was stale and fixing it**, so the "not pushed yet" / "not applied yet" caveats scattered through Section 4/8 wouldn't mislead a future session into re-doing (or worse, skipping) something that already happened. **Next queued, not started:** the session-17 audit findings — highest priority is `readJsonBody`'s missing request-size cap (DoS risk) and the dead `pruneExpiredNonces` (nonce map grows unbounded); the client's complete lack of committed automated tests is the other big one, see item 16 in the roadmap below.

**Just landed (session 17) — a polish push run as 5 parallel worktree agents, merged by hand:** the user said "let's make the game actually polished before we move on" and asked for 4 build tasks plus a 5th read-only audit, run as a swarm. All 5 launched in isolated `git worktree`s simultaneously (extending the pattern session 5 first used for 3 parallel tasks):
1. **Card-frame + mobile redesign** (client) — see Section 3.
2. **Multiplayer reconnect** (server/shared/client) — see Section 5.
3. **Weekly rewards / Achievements / Ranked / Events** (server/client) — see Section 4, closing out spec.md Section 21's Coins-earn loop entirely.
4. **Secrets/hand redaction fix + bot self-damage-risk model** (shared/server/engine, two independent fixes) — see Section 5 and Section 2.
5. **A full read-only app audit** (security, code quality, test coverage, UX, performance, docs) — no code changes, a findings report. Top items: `readJsonBody` (`httpApi.ts`) has no request-size cap (a real memory-exhaustion DoS vector on the single process holding all live match state); `pruneExpiredNonces` (`auth.ts`) is written but never called, so the nonce map grows unbounded; **the client has zero committed automated tests** — every "verified with Playwright" note throughout this whole document was a one-off throwaway script, never persisted, so there's no regression safety net; `useOnlineMatch.ts` had no WebSocket `onclose` handler (flagged mid-audit directly to the reconnect task, which fixed it as part of task 2 above). Also flagged, lower urgency: transaction-wrapper boilerplate duplicated 6× (a `withTransaction` helper would collapse it — timelier than ever now that task 3 added a 7th copy of the pattern); no index backing the public Leaderboard's `coin_transactions.reason` queries (full table scan per page view); zero rate limiting anywhere including SIWE auth; Postgres TLS cert verification disabled by default; zero ARIA anywhere in the client. What's solid: all SQL parameterized (no injection risk anywhere), every money-moving op correctly locks+transacts (no TOCTOU), consistent 404-not-403 on cross-account access, strict TypeScript with no stray TODOs. **None of the audit findings have been acted on yet** — surfaced this session, not yet scheduled.

**The merge itself was the hard part**, not any single task: reconnect (task 2), the redaction fix (task 4), and the achievement/rank hooks (task 3) all touch `server/src/matchRoom.ts`, and the redaction fix also changed `shared/src/index.ts`'s `serializeState` signature (added a required `viewerId` parameter). Merged sequentially into an integration branch (card-frame → reconnect → redaction+bot-risk → weekly/achievements/rank/events), by hand, in that order, with real conflict resolution at each step rather than taking either side wholesale — two real cross-branch bugs caught this way that neither individual task could have seen in isolation: (1) task 2's new `MatchRoom.reconnect()` called the pre-redaction one-argument `serializeState(this.state)`, which would have failed to typecheck once task 4's required `viewerId` parameter landed — fixed to pass the reconnecting player's own id; (2) the two branches' conflicting edits to `MatchRoom.start()` needed combining by hand (task 2's `reconnectToken` field + task 4's per-viewer `serializeState` calls), not picking one side. Full verification after every merge step, not just at the end: 56/56 engine tests, 122/122 server tests (real local Postgres, migrations `0001`–`0007` applied fresh), clean `tsc --noEmit` across all four packages, clean client `vite build`. Merged into local `main`, pushed to `origin/main`, and migration `0007` confirmed applied to production Postgres (session 18: `railway connect Postgres --tunnel-only`, then `npm run migrate --workspace=server` reported "Already up to date" — checked `_migrations` directly to confirm rather than trust that message blindly, and all 7 migrations plus the new tables (`achievement_progress`, `events`, `rank_points_transactions`, etc.) are there). Both Vercel and Railway confirmed responding 200 live. Session 17's whole polish push (card frame/mobile, reconnect, weekly/achievements/rank/events, Secrets redaction + bot risk model) is genuinely live now, not just merged locally.

**Session 16:** a game balance/mechanics pass, driven by real bot-vs-bot playtesting data rather than guesswork — the user reported the game "seems mindless" and flagged Degens as feeling overpowered, and asked for research into Hearthstone/Yu-Gi-Oh mechanics. Landed: Deathrattle (`onDeath`), Hearthstone-style Secrets (a new `CardType`, not full Yu-Gi-Oh traps — the turn structure doesn't support a priority/response window), a Silence mechanic/card, one 6-8 cost Legendary finisher per faction (raising the pool's max cost from 5 to 8, closer to the 10-Energy ceiling), and a balance pass trimming Normies/Builders and buffing Crypto Bros. See Section 2 for the full writeup, including the reconciled Degens finding (weak in bot play, strong in human play — a bot-AI self-damage-risk gap, not a card-balance failure) and before/after win-rate numbers. 53/53 engine tests green (12 new), `tsc --noEmit` clean, `card-schema.md` updated with a new Secrets section. Followed up with a small client addition: an armed-Secrets "🔒 N" badge on `PlayerHeader.tsx`, closing the UI gap `card-schema.md` Section 8.2 flagged. Committed (`a6381e3`, `36a67e3`).

**Session 15:** attack-lunge animation + a win/lose result overlay with "ticker tape parade" confetti, requested directly by the user after seeing session 14's sound/VFX pass ("when you attack the cards actually move," "after win or lose you should get a popup maybe confetti"). Building the lunge surfaced and fixed a real bug: session 14's `useMatchSounds` was silently inert for Play vs AI the whole time (only the direct-click UI sounds were actually firing) because `useMatch.ts` mutates its `MatchState` in place and never hands back a new object reference, so a `useEffect([state])` dependency never re-triggers past mount — fixed by keying on primitives (`log.length`/`winner`/`activePlayer`) instead, the same pattern the session-5 hit-flash mechanism already used correctly. See Section 3 for the full writeup, including how the lunge direction is derived without any engine change. Client type-checks/builds clean; verified with a scripted-browser run that polls across a full match rather than one narrow window (the mistake that let the sound bug through unnoticed last session). Committed (`2de61fe`).

**Session 14:** sound effects and a fuller VFX pass, requested directly ("make it more fun... sound effects and visual effects") while branding image-generation credits were regenerating. A Web Audio synth engine (`sound.ts`, no audio assets/licensing needed) covers every meaningful match/reward moment; damage/heal number popups, a Market Event screen flash, and a critical-Volatility pulse round out the VFX side. See Section 3 for the full writeup. Client type-checks/builds clean, verified with a scripted-browser run that patched `OscillatorNode.prototype.start` to confirm sounds actually fire without needing real audio playback. Committed (`8b4be4e`).

**Session 13:** the flagship PFP went photorealistic — the user liked the retro-trader character concept from session 12 but wanted the execution more realistic, and supplied a reference render (via Grok, not Pixa) hitting the mark: same character (hair curl, pinstripe suit, green/gold tie, brick phone, gold rings) but studio-lit photoreal instead of cartoon-flat. Adopted directly as `branding/assets/pfp.jpg`; the original flat-vector PFP kept as `pfp-flat-vector-archive.svg`/`.png` (still useful for a small/scalable icon context, just not the primary social identity anymore). **Consequence, not yet resolved:** `banner.svg`/`.png` is still the old flat-vector style and now visibly mismatches the photoreal PFP — needs regenerating with `pfp.jpg` as an image-to-image reference (not a fresh text prompt, which won't reproduce the same face) once there's more Pixa credit budget; currently sitting at 8 credits, below every model's per-image floor (Flux, the photorealistic-capable option, costs 12-14/image). `branding.md` Sections 5/8 have the full writeup and the exact regeneration technique to use next. Committed (`fdf0d90`).

**Session 12:** the session-11 Doggos-mascot asset pack got rejected on review ("i dontt vibe with" the images) — replaced with a retro hedge-fund-manager human character instead, after the user pointed at CryptoPunks/BAYC/Milady/Ponke and specifically a "slick Wall Street trader" avatar style (referencing accounts like Al Dunlop) currently landing well on Robinhood. Explored 3 takes (moody/sinister, cartoonish/collectible, sleek/heroic), compared two of them by compositing the raw generations into an actual banner-with-wordmark mockup via a throwaway HTML page + Playwright screenshot (zero extra Pixa credits — worth reusing this technique before spending credits on a full regeneration), and landed on the sleek/heroic take — its dark suit blends into the banner background instead of sitting on top like a sticker. Final assets: 1 PFP + 1 full-scene banner (a giant hero-pose figure on a glowing floor grid — nice unplanned synergy with the "the floor is the battlefield" tagline). The rejected dog pack (PFP, 2 banners, 4 stickers) was fully retired, not kept as a fallback. Ran low on Pixa credits mid-exploration (8 left as of this session) — stickers for the new character are the next thing to generate once there's more budget. Also surfaced, not yet resolved: **the user is reconsidering the game's actual name** ("Crypto Clash"), separate from the visual-identity work — flagged in `branding.md` Section 1 as an open question everything else should avoid depending on (don't bake the wordmark into anything hard to redo) until it's settled. `branding.md` Sections 5/8 have the full writeup. Committed (`1989daf`).

**Session 11:** a brand guide (`branding.md`) plus a first generated social-asset pack (`branding/assets/`) — the user asked for both, in that order: document the visual identity already established (session 8's "the floor is the battlefield"), then generate real assets for social media before website/card art. Used the Pixa MCP image-generation tools (`recraft-v4-vector`) for the first time on this project. Landed on a Doggos-flagship mascot — a golden dog in trading-floor attire (white shirt, loosened terminal-green tie) — reasoned from `STATUS.md`'s existing token/community direction ("dogs being the proven meme-coin lineage," Section 0) rather than inventing a new character lineage. Generated and user-approved: 1 profile picture (self-contained circular badge), 2 banner compositions (primary: a rain-slicked hero shot; kept a second, more playful alternate), and 4 die-cut stickers (thumbs-up, wave, shocked/panic, sunglasses). One real iteration loop: the first sunglasses sticker came out with pointy ears instead of the mascot's established floppy ears — caught during review, regenerated with the mismatch called out explicitly in the prompt, now consistent. Every asset saved as both `.svg` (Recraft's real vector output — scales cleanly for anything from a sticker to a print banner) and `.png` (quick-preview raster) — `branding.md` Section 8 has the full asset table and usage notes. Committed (`7e8de8c`).

**Session 10:** a real multi-wallet picker (EIP-6963) — the user reported "I still can't select which wallet I want to sign in with" after session 7's fix, which only solved switching *accounts* within one already-detected extension, not choosing *between* multiple installed extensions (MetaMask vs. Coinbase Wallet vs. others). See Section 3 for the full writeup. Client-only change (no server/migration involved); verified with a scripted-browser test simulating two independently-announcing wallets. Committed (`47a1615`).

**Session 9:** a Referrals/invite system — "some sort of social invite feature eg free packs or free card to get the viral spread going," the user's idea. Both sides get a free pack (referred immediately, referrer once their friend plays a real match); see Section 3/4 for the full writeup. Also: the chain-choice discussion landed on leaning Robinhood Chain over Solana (see Section 0/`architecture.md` Section 8) — a direction, not a build, per the existing "don't build on-chain before the game/economy is proven" guidance. 88/88 server tests green, verified end-to-end with a 3-wallet scripted-browser run. Migration `0006` applied to production. Committed (`74c1a23`).

**Session 8:** a real visual identity + a landing page — "the trading floor is the battlefield," raised by the user ("the UI looks very basic and very AI"). New design tokens (dark bullpen palette, Chakra Petch/IBM Plex Mono fonts) cascade into every existing screen since ~118 stylesheet rules already used CSS custom properties; a new `LandingPage.tsx` (lit-skyline hero, scrolling ticker, community section with clearly-marked placeholder links) is now the app's default screen. See Section 3 for the full writeup. Client type-checks and builds clean; verified visually with real Chromium screenshots across desktop/mobile, landing/menu/leaderboard/match. Committed (`ab05e4d`).

**Session 7, third wave:** a Leaderboard — Most Wins / Win Rate / Coins Earned, public to view, "you're ranked #N" once connected — user's idea mid-session. Pure read-side aggregation over the existing Coins ledger, no migration needed. See Section 3/4 for the full writeup. 78/78 server tests green, scripted-browser verified across all three tabs both logged out and in. Committed (`eb26cfa`).

**Session 7, second wave:** Coins earn loop, part 2 — daily login rewards (a 7-day escalating, cycling streak) and 3 daily quests (Play 1/Play 3/Win 1 match), the reasons-to-come-back-today half of spec.md Section 21 that match results (session 5) and the welcome bonus didn't cover. See Section 4 (server: `dailyRepo.ts`, `questsRepo.ts`, migration `0005`) and Section 3 (client: `QuestsScreen.tsx`). Also fixed, raised mid-session by the user: wallet-switching — `useWallet.ts` had no way to pick a different account once a site was already authorized in MetaMask, and no listener for the active account changing outside the app; added a "Switch Wallet" action (forces the picker back open via `wallet_requestPermissions`) and an `accountsChanged` listener that drops a now-stale session rather than silently continuing to act as the old account. Migrations `0004` (crafting, session 6) and `0005` (this session) were also both applied to the production Railway Postgres this session — confirmed via `railway connect Postgres --tunnel-only`, so Crafting and Quests actually work on the live site now, not just locally. 72/72 server tests green (real local Postgres), full scripted-browser (Playwright) verification of the Quests screen + wallet-switch/disconnect flow. Committed (`e6792ed`).

**Just landed (session 6, second wave):** crafting — a real duplicate-protection loop (spec.md Section 18) letting a player disenchant duplicate Uncommon+ cards into Dust and craft the ones they actually want, closing the loop packs/foils opened up. See Section 4 (server: `craftingRepo.ts`, migration `0004`) and Section 3 (client: `CraftingScreen.tsx`). Code review (first attempt stalled/failed after 600s and was retried) caught one real bug — `POST /api/craft/disenchant` only checked `count` truthiness, so a negative count skipped past validation into an untyped `Error` that surfaced as an unhandled 500 instead of a clean 400 — plus two defensive/cleanup fixes taken on top: the disenchant instance-selection query now also protects any future non-standard edition the same way it already protected foils (nothing grants First Edition/Legendary/Genesis instances yet, but the query no longer assumes that stays true), and the Coins/Dust "cached balance column + audit table" delta logic — previously copy-pasted once already — got pulled into one shared `server/src/ledger.ts` instead of a third near-duplicate. Also surfaced and fixed, unrelated to the review: `db.test.ts` and `api.test.ts` both wipe the whole `accounts` table in their hooks and vitest runs test *files* in parallel by default, so they occasionally raced each other's fixtures — confirmed directly (an unmodified rerun flipped a failure straight to a pass) and fixed with `fileParallelism: false` in `vitest.config.ts`. Committed (`a7fe37b`) and pushed to `origin/main`.
**Session 6, first wave:** the pack/rarity economy design pass — real weighted rarity odds replacing the placeholder table, plus a new independent foil mechanic — per Section 3/4/17, including a code-review pass that caught and fixed a real bug (the foil shimmer CSS was silently dead — an inline `style` prop was clobbering it) before this shipped. Committed (`76ecbb9`, `d1f8231`) and pushed to `origin/main` — Vercel/Railway auto-deploy on push, so this (and everything from session 5, which turned out to already be on `origin` by the time this session started) should be live shortly.

**Session 5, second wave:** three more roadmap items landed in one batch — a real Coins-earn loop for Play Online wins/losses/draws (Section 5), a meaningfully smarter bot opponent (Section 2), and a client polish pass covering hit feedback, keyword tooltips, interaction polish, and a small-viewport mobile fix (Section 3). Built as three parallel, isolated `git worktree` agent tasks (avoiding file-overlap by construction — server/coins vs. engine/bot vs. client/visual, with explicit boundaries where two touched the same file), each independently reviewed and verified before merging: real Postgres integration tests plus a real WS-level match played to a genuine win for the Coins loop, 45/45 engine tests plus a 200-game bot-vs-bot sanity sim for the AI, and a real scripted-browser run confirming the hit-flash/tooltip/mobile-layout work for the polish. All three merged into `main` cleanly (one trivial import-order conflict, hand-resolved).

Roadmap, in rough order (collectibility now prioritized ahead of AI/polish per Section 0's direction):

1. ~~Finish the card layer (all 6 factions + Items)~~ — done.
2. ~~Wire content into the live game (deck selection)~~ — done.
3. ~~Accounts + persistent deck builder (off-chain, no rarity yet)~~ — done, including production DB provisioning and a live-site end-to-end verification (Section 1/4).
4. ~~Rarity & editions~~ — done: data model, starting-collection grant, ownership-gated deck building.
5. ~~Packs~~ — done, including a real economy design pass (session 6): tuned rarity odds replacing the earlier placeholder, plus an independent cosmetic foil roll. Still open: the exact odds/foil-rate numbers are a first design pass, not backed by live telemetry yet; only one pack type exists (no premium/Genesis packs); First Edition and Genesis remain deliberately outside the pack RNG entirely, with no grant mechanism of their own built yet (spec.md Section 17).
6. ~~Collection screen~~ — done: owned/missing by faction/rarity, filterable, with a summary progress bar. Foils are now real and surfaced (session 6) — a count, a per-card badge, a filter. First Edition/Serial filters (spec.md Section 19) still not exposed — nothing populates those `card_instances` fields yet.
7. ~~Crafting~~ — done (session 6): disenchant duplicate Uncommon+ cards into Dust, craft the ones you want, closing spec.md Section 18's duplicate-protection loop. Same "first design pass, not tuned" caveat as pack odds — the Dust values are anchored to a real precedent (Hearthstone) but not backed by this game's own play data yet.
8. ~~Smarter AI~~ — done: real lethal/clean-kill/trade evaluation and aura-aware placement in `bot.ts`, not just "play anything, attack face."
9. ~~Client polish pass~~ — done. Session 5: hit feedback, keyword tooltips, interaction polish, one mobile breakpoint fix. Session 8: **art direction decided and applied** — "the trading floor is the battlefield" (dark bullpen/exchange aesthetic, new fonts/palette, a landing page) — see Section 3. Session 14: **sound effects (a synth engine, no assets needed) and a fuller VFX pass** (damage/heal number popups, a Market Event screen flash, a critical-Volatility pulse) — see Section 3. Session 15: **attack-lunge animation and a win/lose result overlay with confetti** — see Section 3. Session 17: **a real Hearthstone-style card-frame redesign and a full mobile pass** (375–768px) — see Section 3. Still open: real sound *assets* (currently synth-only, a deliberate choice per the user, not a gap).
10. ~~Coins earn loop, part 2~~ — done (session 7: daily login rewards + 3 daily quests; session 17: weekly rewards, achievements, ranked progression, and events, closing out spec.md Section 21's full source list — see Section 4). Still open: every reward number across the whole Coins loop is a first-pass placeholder, not tuned against real play data, and ranked has no season/reset design yet.
11. **On-chain layer** — minting (opt-in, async, per architecture.md Section 8), marketplace, wallet-as-asset-custody. Deliberately last — chain choice now leans Robinhood Chain (session 9, see Section 0) but isn't locked in, and architecture.md Section 12 is explicit that building this against a game/economy that doesn't exist yet is the standard crypto-gaming failure mode.
12. ~~Multiplayer robustness~~ — reconnect-to-in-progress-match done (session 17, see Section 5). Still open: less naive matchmaking than FIFO — the session-17 reconnect task's explicit stretch goal, not attempted (reconnect was prioritized).
13. ~~Leaderboard~~ — done (session 7): Most Wins / Win Rate / Coins Earned, public to view. Not part of the original roadmap — added because the user asked for it mid-session, same pattern as wallet-switching (item logged in Section 3/4 rather than numbered from the start).
14. ~~Referrals / invite system~~ — done (session 9): free packs for both sides of an invite, the referrer's gated on a real completed match. Also not part of the original roadmap — the user's idea, same "logged where it landed" pattern as items 9/13.
15. ~~Game balance & mechanics pass~~ — done (session 16): Deathrattle, Secrets, Silence, per-faction top-end finishers, and a data-driven balance pass (Section 2). Not part of the original roadmap — the user's direct feedback that matches "seem mindless," same "logged where it landed" pattern as items 9/13/14. Session 17 partially closed the "bot AI has no self-damage-risk model" gap flagged here (Degens' bot win rate 37.5%→40.0% — real but modest; the dominant remaining Degens bot-loss pattern turned out to be a broader trade-evaluation/board-recovery gap, not self-damage specifically, see Section 2). Every new number here (finisher stats, trim amounts) is still a first balance pass, not tuned against real human play data.
16. ~~App-wide audit findings (session 17, item 5 of the polish push)~~ — done (session 19): request-size cap, nonce pruning, basic rate limiting, transaction-boilerplate de-dup (11 sites → 1 helper), the missing Leaderboard index, a real client test suite (37 tests, was zero), and an ARIA pass across 22 components — see Section 8's session-19 writeup for the full breakdown. **TLS certificate verification was tested directly against production and found not to work as a simple flag flip**: setting `DATABASE_SSL_VERIFY=true` on the live Railway server broke every DB-backed endpoint within about a minute (`self-signed certificate in certificate chain`) — Railway's managed Postgres presents a self-signed cert, so Node's default trusted-CA store rejects it. Reverted immediately, confirmed `/api/leaderboard/wins` back to 200. Left `false` (today's existing behavior) as the default; re-enabling it for real needs Railway's actual CA supplied via `NODE_EXTRA_CA_CERTS` (or `ssl.ca`), not attempted this session — see `server/src/db.ts`'s comment for the confirmed finding.

Check the conversation, not this list, for what's actually being worked on right now.
